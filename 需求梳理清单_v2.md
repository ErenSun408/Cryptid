# 需求梳理清单 v2

> **更新日期**：2026-08-16（客户确认 Q-01 / Q-02 / Q-03，见第四节）
> **相对 v1 的变化**：v1 的疑问中有 9 项已通过调研或实测得出确定结论，
> 3 项结论被推翻需要改设计，另新增 4 项此前未识别的问题。
> 本版所有技术结论均附依据（官方源码行号、官方文档、或本地实测数据）。
>
> **2026-08-16 增补**：客户已就否认性取向、丢卡处置、目标客户三项拍板。
> **最高风险（国密合规）已解除**，VeraCrypt 技术选型就此锁定。
>
> **2026-08-17 增补**：N-02 重写 + 客户确认 Q-06（选 A）。
> 否认性由押在「卡支持多 PIN 分区访问」改为押在**密钥派生**上（隐藏卷密码不落卡，
> 由卡内密钥块 + 用户口令派生）。**USB 卡采购不再是 Q-01 路线的阻塞项**，
> 普通安全卡即可，多 PIN 降级为可选纵深防御。
>
> **2026-08-17 再增补（重要）**：新识别 **N-05 —— "加密盘"不能是 VeraCrypt 外层卷**，
> 改为**双容器架构**（A = 普通卷做加密盘，B = 外层/隐藏卷做诱饵与真隐藏盘）。
> 同时撤销 A-02 的"已解决"标记：全自动挂载与否认性不可兼得，
> **客户已通过 Q-07 拍板改为手动输入密码**，登录界面恒为单一密码框。
> 连带：《基本需求》第 1、7 条需改措辞；**P-01 的性能余量结论需按双容器重算**。
>
> **2026-08-17 三增补**：新增**第八节**，整合三模式完整设计并新增两项关键结论 ——
> **分档设计**（两个容器**独立创建**、隐藏盘容器可后补，天然形成三档形态；
> 若人人都有诱饵结构则否认性归零）与
> **三层不可区分要求**（磁盘／UI／**本地状态**，其中本地状态层此前从未识别，是最易疏漏的击穿点）。
> 同时订正 Q-07 的密码派生（容器 A 改为仅从卡派生，原写法会导致容器 A 在一种模式下打不开），
> 并补齐 N-05 的源码依据、闭环 v1 遗留的 G-01 / G-03。
>
> **2026-08-17 四增补（Spike 6 已完成）**：见 `spike/SPIKE6-双容器挂载性能.md`。
> **P-002 五秒预算确认成立**（模式 A 3.63s，余量 1.37s），性能风险点解除。
> 但发现两件事：🔴 **挂载必须显式指定 PRF**（自动探测下隐藏卷单次 6.6s，端到端 8.9s 爆表）；
> 🔴 **并行挂载经实测无效，该缓解手段作废**。
> Argon2id 定参 **m=512MiB/t=2/p=1**。另订正 Spike 2 一处记录（模式 B 报告 `type=0` 而非 `type=2`）。
>
> **2026-08-19 六增补（Spike 3b 已完成）**：见 `spike/SPIKE3B-建卷工具编译验证.md`。
> **里程碑 1、2 均通过** —— 改造版 FormatDLL 无界面建出隐藏卷容器，
> 三种挂载方式 `type` 分别为 1/0/2、容量符合预期。改动仅 2 文件 30 行，未触碰 `Tcformat.c`。
> **N-01 的解决方案已由端到端实测证实，P1 建卷工具的技术不确定性解除。**
> 附带新增 🔴 **N-08 —— 建卷路径需要管理员权限**（`FSCTL_GET_VOLUME_BITMAP` 要求提权），
> R-02「无需管理员权限」适用范围随之收窄；
> 且**档二不提权、档三提权会使 UAC 行为本身泄露档位**，须改为建卷流程无条件提权。
> **N-08 已于同日完成管理员权限补验并定案**：提权后 FSCTL 成功，
> 且碎片化测试实测朴素估算在碎片场景**高估 40.9%**，
> 据此**模式 C 亦定为必须提权**（原为待决策项）。
> `ChangePwd()` 无界面可用性亦已实测通过，N-06 的升档改密码方案无障碍。
> **Spike 3b 全部完成，无阻塞项。**
>
> **2026-08-19 五增补**：**Q-04 结案** —— 定为**明文导出，用户自担风险**（见第四节 Q-04）。
> 新增两项此前未识别的设计缺口：
> 🔴 **N-06 —— 档一（默认档）无认证锚点**，容器 A 的密码不依赖用户输入，
> 该档下登录密码形同虚设，输任何密码均可登录；
> 🔴 **N-07 —— Q-01 硬性需求 1（模式 B 后在隐藏卷写时间戳）在现行密钥派生设计下不可实现**
> （模式 B 物理上不持有隐藏卷密码），改为「隐藏卷完整性校验 + 卡上单调计数器」双机制。
> 另补齐 **Q-07 的挂载顺序要求**（先 B 后 A）与 **8.5 模式 C 缺失的第二个密码输入**。
>
> **2026-08-19 七增补：Q-05 结案，所有待客户确认项全部关闭。**
> 客户答复：① 范围**保持现状**（以《基本需求》9 条为准）→ 排期基线与
> "不写内核驱动"路线坐实，第五节删除建议可执行；
> ② 第 5 条"明文"确为 **(a) 导出解密后的原始文件** → **Q-04 四项风险全部生效，无退路**；
> ③ 第 1、3 条"与/以及"**不要求**两盘符同时在线 → 设计无影响
> （但**模式 A/B 盘符数必须一致**是否认性要求，不得据此放松）。
>
> **✅ Q-01 ~ Q-07 全部确认完毕，无待客户答复项。**

---

## 零、最重要的前提变化

客户新提供的《基本需求》（9 条）相比原《需求说明书》**范围大幅缩减**。以下内容在新需求中**未出现**：

- 隐藏分区模式一（改分区类型 ID）与模式二（UD 隐藏区，任意 LBA 偏移）
- 增量备份、脏块跟踪、断点续传、分卷存储、"备份数据完全随机无特征"
- 自研 KMDF 内核驱动 → **因此不需要 WHQL 认证**

第 9 条"微软认证签名"指的是 **EV 代码签名证书**（数千元、2-3 周），
与 WHQL 驱动认证完全是两回事。

~~**这三块正是原方案中周期最不可控的部分。请客户书面确认以《基本需求》9 条为准**，
若仍需原需求书中的隐藏分区双模式，工期与风险需重新评估。~~

> **✅ 2026-08-19 客户已确认「保持现状」，即以《基本需求》9 条为最终范围（Q-05）。**
> 上述三块内容正式确认不在范围内，**排期基线与"不写内核驱动"的技术路线就此坐实**。
> 第五节的删除建议随之可执行。

### 由此确定的技术路线

| 层 | 方案 | 许可证 |
|---|---|---|
| 建卷 | VeraCrypt Format SDK（DLL） | Apache 2.0，**可闭源** |
| 挂载/卸载 | 直接向官方驱动发 IOCTL | 接口调用，**非衍生作品** |
| 隐藏卷创建 | 独立开源小工具（见 N-02） | TrueCrypt License 3.0，需开源 |
| 主程序 | C++ 核心 + WebView2 前端 | **100% 闭源，可混淆** |

**关键点：我方不编写任何内核驱动。**

### 卷形态：双容器架构（2026-08-17 确定，见 N-05）

| | 形态 | 对应《基本需求》 | 日常写入 |
|---|---|---|---|
| **容器 A** | 普通卷（独立文件） | **加密盘** | ✅ 安全 |
| **容器 B** | 外层卷 + 隐藏卷 | 外层 = 诱饵"隐藏盘"；隐藏卷 = **真·隐藏盘** | 仅隐藏卷 |

**加密盘不是外层卷。** 把外层卷当日常盘用会因 Q-01 的"模式 B 不开保护"而持续威胁隐藏卷。

**两个容器独立创建、各建各的**（与 VeraCrypt 一致），隐藏盘容器可后补。
由此天然形成三档形态（只建加密盘 / 隐藏盘容器为普通卷 / 隐藏盘容器含隐藏卷），
后两档在磁盘上不可区分 —— 这是否认性的落点，见 **8.3 / 8.4**。

---

## 一、已解决的问题

### L-03 ✅ VeraCrypt SDK 确实存在，但只有一半

**结论**：官方仓库 `github.com/veracrypt/VeraCrypt-SDK` 真实存在，但**只有 Format SDK**
（建卷、格式化分区），**没有 Mount SDK**。README 原文："Additional SDK modules
(such as Mount SDK) are **planned for future releases**"。

**影响**：F-046 中"通过 SDK 实现挂载"这条路不存在，挂载必须走 IOCTL（见 L-01）。

**附带信息**：SDK 唯一正式 release 是 1.26.24（2025-06-13），master 分支已对齐
1.26.29 但未发新 tag。仅提供 **x64 与 ARM64** 预编译 DLL，**无 32 位**。

### L-01 ✅ 闭源商业化可行

**结论**：按上表的技术路线，**主程序可以完全闭源并做代码混淆**，S-006 成立。

依据：
- Format SDK 仓库为**纯 Apache 2.0**，LICENSE 不含 TrueCrypt License 3.0，
  且提供预编译 DLL —— 链接使用不产生传染。
- IOCTL 是与**独立安装的**程序通过操作系统标准接口通信，不链接、不编译其源码，
  不构成 TCL 3.0 意义上的衍生作品。

**遗留法务事项**：`Apidrvr.h` 头文件本身受 TrueCrypt 许可，**不得直接 `#include`**，
必须按接口自行声明结构体（clean-room）。本项目 `spike/vcspike.c` 已按此实施并验证可行。
**此点仍需律师出具意见书。**

### A-02 ⚠️ 部分解决 —— 技术能力成立，但与"全自动挂载"不可兼得（2026-08-17 修订）

**技术层面 ✅**：`MOUNT_STRUCT` 具备隐藏卷相关字段，同一容器可用两个密码挂出两套完全隔离的数据。
**已实测通过**，详见第六节。

**⚠️ 2026-08-17 撤销"已解决"标记**：v1 原文 A-02 的完整表述是
「**"似是而非的否认"在自动挂载设计下基本失效** —— 胁迫场景下卡+PIN 一旦交出，
隐藏卷跟着一起挂载，**没有"只交外层"的操作空间**」。

v2 此前只回答了前半句（VeraCrypt 能否做到双密码 → 能），
**未回答后半句（全自动挂载下用户如何表达"我正在被胁迫"）**。

《基本需求》第 1 条要求"插上 USB 卡，应用软件打开登录成功自动加载"、
第 7 条要求"登录密码保存在 USB 卡中"——若严格执行，卡一交出软件即自动挂出真实隐藏盘，
**用户没有任何操作空间可以只交出诱饵，Q-01 的整套否认性设计落空**。

**结论：全自动挂载与 Q-01 的保隐藏路线，二者只能选一个。**
**✅ 客户已于 2026-08-17 拍板选择手动输密码，见 Q-07。**

### A-04 ✅ 安全卡定位已明确：安全存储

《基本需求》第 7 条"登录密码、加密盘、隐藏盘密码都保存在 USB 卡中"——
明确是**存储**而非**运算**。

**因此 S-004"密钥不能以明文形式暴露在内存"无法完全兑现**（见 R-03）。

### V-01 / V-02 ⏸ 降级为 v2.0 议题

v1.0 采用**文件容器**形态，不涉及分区，这两项暂不阻塞。
文件容器天然满足《基本需求》第 2、4 条（已实测，见第六节）。

### V-03 ✅ C-007 大概率可删除

采用官方签名驱动，Win11 BSOD 问题交由上游修复。当前最新版 **1.26.29（2026-06-12）**
在本机 Windows 11 26200 上实测挂载/卸载 6 次无异常。
**建议**：在目标系统版本上做一轮压力测试确认后删除此需求，可省下大量内核调试工作量。

### P-01 ✅ 性能余量充足

实测挂载 **1.42~1.47 秒**，卸载 **73~106 毫秒**（AES/SHA-512，PIM 默认，100MB 容器）。
需求 P-002 要求"认证到挂载 ≤5 秒"，**余量约 3.5 秒**，足够覆盖 PIM 提高与读卡通信开销。
S 安全性与 P 性能的矛盾在当前参数下不存在。

> **✅ 2026-08-17 已由 Spike 6 实测复核，结论成立但前提变了。**
> 详见 `spike/SPIKE6-双容器挂载性能.md`。
>
> **上方 1.42~1.47s 是 PRF 自动探测下的普通卷数据，不能直接外推。**
> Spike 6 实测：**挂载时显式指定 PRF（SHA-512）后**，普通卷降至 0.99s、隐藏卷 1.74s；
> 若沿用自动探测，隐藏卷需 **6.6 秒**，单这一步就超预算。
>
> **模式 A 端到端合成（进程内折算）**：Argon2id 1.04s + 容器 A 0.92s + 容器 B 1.67s
> = **3.63s，余量约 1.37s** 供读卡通信。模式 B 更宽松（2.79s）。**P-002 成立。**
>
> 🔴 **两项硬要求**：（1）挂载必须显式指定 `pkcs5_prf`，禁止用 0；
> （2）原缓解手段"两容器挂载并行化"**经实测无效已作废**（串行 2.61s vs 并行 2.55s，差异在噪声内）。

### D-01 ✅ 建议删除 x86 32 位支持

Format SDK 仅提供 x64/ARM64。Windows 11 亦无 32 位版本。C-004 建议删除。

### T-02 / T-03 ✅ 大幅简化

不做内核驱动 → 不需要 WHQL → PK-008"注册 Windows 硬件开发人员计划"可删除。
仅需 EV 代码签名证书。T-02（硬件 Token 签名 SOP）仍需保留但影响面小得多。

---

## 二、结论被推翻，需要修改设计

### R-01 🔴 隐藏卷保护模式**不能**用于胁迫场景

**此前的错误设计**：PIN-B → 挂外层卷 + 开启隐藏卷保护，作为胁迫逃生路径。

**为何错误**：VeraCrypt 官方文档 [Protection of Hidden Volumes] 明确警告：

> "When an adversary asks you to mount an outer volume, you of course must **not**
> mount it with the hidden volume protection enabled."
>
> 保护激活期间对手 "**can** find out that a hidden volume exists within the outer volume"。

**本地实测佐证**：向开启保护的外层卷写入 190MB 后，应用层写入全部"成功"，
但卸载后文件全部消失，可用空间仍显示 8.0MB/198.2MB —— **空间被占用却无文件引用**。
这种反常行为足以让对手判断存在隐藏卷。

**修正后的三模式设计**：

| 模式 | 用途 | `bProtectHiddenVolume` |
|---|---|---|
| A. 正常使用 | 隐藏卷密码，访问真实数据 | — |
| B. **胁迫交出** | 挂外层卷 | **必须 FALSE** |
| C. 维护诱饵 | 用户自行更新诱饵文件 | TRUE，仅在确认无人观察时 |

> **2026-08-17 修订（N-05 双容器架构）**：上表仅描述**容器 B**（诱饵容器）。
> 容器 A（加密盘，普通卷）在模式 A 与模式 B 下**均正常挂载**，不涉及隐藏卷保护。
> **由此 Q-01 接受的数据损失风险缩小为：仅波及隐藏盘，加密盘日常数据完全安全。**
>
> **➡️ 三模式的完整定义（含保护模式机制、操作场景、驱动依据）已移至第八节，实现以该节为准。**
> 挂载矩阵与密码派生见 **Q-07**。

**必须让客户知情的代价**：模式 B 不开保护时，对手若向外层卷写入数据，
**隐藏卷将被静默覆盖，真实数据永久丢失**。

这是否认性无法回避的固有权衡：**要么保住数据但暴露隐藏卷，要么保住隐藏性但可能丢数据**。

**✅ 客户已于 2026-08-16 拍板：选择「保隐藏」并接受丢数据的风险（见 Q-01）。**
三模式设计正式生效，以下三项由"建议"升级为**硬性需求**：
1. ~~模式 B 触发后，在**隐藏卷内部**写入时间戳，下次正常登录时告警~~
   🔴 **2026-08-19 作废，见 N-07** —— 模式 B 物理上不持有隐藏卷密码，写不进去。
   已改为「隐藏卷完整性校验 + 卡上单调计数器」双机制，**告警需求本身不变**
2. 外层卷预置足量诱饵数据，降低对手需要写入的动机
3. 产品文档与首次使用引导中明示此风险

### R-02 ✅ 无需管理员权限（正面推翻）

**此前判断**：挂载需要管理员权限，需设计 UAC 提权时机。

**实测结论**：`version` / `mount` / `list` / `unmount` **在普通用户权限下全部成功**。
原因：驱动 IOCTL 声明为 `FILE_ANY_ACCESS`（`Apidrvr.h:27`）。

**影响**：
- 主程序日常运行无需 UAC 提权，用户体验大幅改善
- 符合技术约束"运行时尽量降低权限"
- **排期中"设计提权时机"这项工作可以取消**

**边界**：此结论仅覆盖文件容器。v2.0 裸分区形态涉及物理磁盘访问，需重新验证。

> **🔴 2026-08-19 适用范围收窄（不是推翻）**：本条实测的是
> `version / mount / list / unmount`，**该部分结论完全成立，日常登录挂载确实不需要 UAC**。
> 但**建卷是另一条链路，R-02 未覆盖** —— Spike 3b 实测发现
> 计算隐藏卷最大尺寸所需的 `FSCTL_GET_VOLUME_BITMAP` **要求管理员权限**。
> 详见 **N-08**。排期中"设计提权时机"一项需**部分恢复**，但仅限建卷路径与模式 C。

### R-03 🔴 S-004 表述需修改

"密钥不能以明文形式暴露在内存"**无法完全兑现**。密码必须以明文填入 `MOUNT_STRUCT`
才能传给驱动，任何方案都绕不开（A-04 已确认卡为存储而非运算）。

**建议改写为**：明文驻留时间最小化 + 用后立即 `SecureZeroMemory` 清零 +
禁止使用会静默重分配的容器（如 `std::string`）+ 使用锁页内存防止换页到磁盘。
这样才是可验收的。

---

## 三、新增问题（v1 未识别）

### N-01 🔴 隐藏卷无法通过任何官方接口程序化创建

**这是本次调研最重要的发现之一。**

- `VeraCryptFormatOptions` 结构体**无任何隐藏卷相关字段**（已核对完整定义）
- 官方 Issue #1305 确认：`VeraCrypt.exe` 与 `VeraCrypt Format.exe` 的**命令行同样无法创建隐藏卷**
- 本地实测：命令行参数不被采纳，每次均回退到图形向导（提权后亦然）

**唯一能创建隐藏卷的是 GUI 向导。**

**解决方案（推荐）**：从 VeraCrypt 源码裁剪出**独立的建卷工具 exe**，
单独开源以遵守 TCL 3.0，主程序闭源并通过进程调用它。建卷是一次性低频操作，
进程边界开销无关紧要，且开源的仅是 VeraCrypt 本就公开的建卷代码，
**不含我方任何商业价值**。

**不推荐的替代方案**：自研隐藏卷头写入（需自实现 PBKDF2/Argon2 + XTS，
一个字节写错即数据不可恢复，风险极高）。

#### ✅ Spike 3a 已完成（2026-08-16）：裁剪可行，且远比预估简单

详见 `spike/SPIKE3A-建卷工具裁剪可行性.md`。**纯源码分析，未编译。**

**结论修正**：N-01"隐藏卷无法程序化创建"**对外仍成立**（SDK 与命令行确实不支持），
但准确表述是「官方对外暴露的接口不支持」，**不是核心不支持**。
内部 `FORMAT_VOL_PARAMETERS` 本就带 `hiddenVol` / `hiddenVolHostSize` 字段
（`Common/Format.h:27-52`），`TCFormatVolume()` 对隐藏卷是完整实现
（`Format.c:125-131 / 559-562 / 641`）。核心还已内建 `bGuiMode` 与 `progress_callback`
无 GUI 路径。**裁剪的性质由"重新实现"降级为"重新暴露"。**

**量化结果**：
- 向导独有逻辑仅 **4 个函数、约 320 行**，连续位于 `Tcformat.c:9747-10068`
  （挂载外层卷 → 判定文件系统 → 扫簇位图 → 算隐藏卷最大尺寸）
- `Tcformat.c` 其余约 10460 行全是对话框流程，整块丢弃
- `Format.c` 对 GUI/宿主的依赖仅 **14 个函数、40 处调用点**，且以错误弹窗为主，
  处理方式是"删弹窗、改错误码"
- 其中"只读挂载外层卷"一步可**直接复用 Spike 1 已验证的 IOCTL 挂载**

**补充发现（同日晚，已修正上述方案）**：完整克隆源码后发现主仓库内的
`src/FormatDLL/` —— **这就是 Format SDK 那个 DLL 的源码**（SDK 仓库只发预编译产物，故此前查不到）。

- 官方的无 GUI 做法是**全链接 + 开关关界面**，而非裁剪：`FormatDLL.vcxproj`
  照常编译 `Dlgcode.c` / `Language.c` / `Progress.c` 乃至整个向导 `Tcformat.c`，
  只用 `Silent=TRUE` + `bGuiMode=FALSE` + `hwndDlg=NULL` + `bForceOperation=TRUE`
  四个开关关掉界面（`VeraCryptFormatSDK.cpp:300-302, 350-352`）。
  → **原"最大不确定项（Dlgcode.c 依赖深度）"就此解答：确实深，但官方也不剥，直接整体链接。风险解除。**
- **SDK 不支持隐藏卷只是一行硬编码**：`VeraCryptFormatSDK.cpp:287`
  `volParams.hiddenVol = FALSE; // SDK does not support hidden volumes`
- 那 4 个向导函数随 `Tcformat.c` 一并编译且均非 `static`，**可直接调用，可能连重写都省掉**

**修正后的 3b 方案**：不再从向导裁剪，改为**以官方 `FormatDLL` 工程为起点改造**
（已知可编译、已验证可无界面运行），只需透传隐藏卷两个参数 + 补两阶段编排。
工作量预期低于原方案。详见 `spike/SPIKE3A-建卷工具裁剪可行性.md` 第五点二节。

~~**待办**：Spike 3b 编译验证（需先装 VS Build Tools，本机目前只有 MinGW，无 MSVC）。
**在 3b 通过前不要下调 P1 排期。**~~
源码已完整克隆至 `spike/_vcref/`（143 MB，master 分支浅克隆）。

> **✅ 2026-08-19 Spike 3b 已完成，本项结案。** 见 `spike/SPIKE3B-建卷工具编译验证.md`。
> 修正后的方案（以官方 `FormatDLL` 为起点改造）**已由端到端实测证实可行**：
> - 改动仅 **2 个文件、30 行**（解除 `hiddenVol=FALSE` 硬编码 + 透传两个参数 + 跳过宿主空间检查 + 几何校验），**未触碰 `Tcformat.c` 一行**
> - 无界面建出的容器经 `vcspike` 三种方式挂载，`type` 分别为 **1 / 0 / 2**，容量符合预期
> - SPIKE3A"改造性质是**重新暴露**而非重新实现"的判断得到证实
>
> **P1 的建卷工具不再有技术不确定性，可按此路线排期。**
> 附带发现 **N-08：建卷路径需要管理员权限**。

### N-02 🔴 USB 卡的否认性要求（原「多 PIN 分区访问」，2026-08-17 修订）

为使模式 B（胁迫交出）站得住脚，卡必须做到：
**在模式 B 的 PIN 认证下，对手既读不出隐藏卷密码，也看不出"存在隐藏卷密码"这件事。**

若卡只是简单存储、一个 PIN 通过后全盘可读，则对手拿到卡和胁迫 PIN 后
可直接读出隐藏卷密码，**否认性完全失效**。

#### 一、泄露有两层，只堵住第一层是不够的

《基本需求》第 7 条要求登录密码、加密盘密码、隐藏盘密码**都存在卡里**。
单 PIN 全盘可读的卡会连着漏两层：

1. **密码泄露** —— 对手用胁迫 PIN 打开卡，直接读出隐藏卷密码，隐藏卷当场打开。
2. **存在性泄露（更致命）** —— 即使把值藏住，卡上"有一个隐藏盘密码槽位"本身就是证据。
   否认性要求对手**看不出有第二套东西**，而不是"看得见但读不了"。
   一个 `ACCESS DENIED` 的文件与一个明文密码，对否认性的破坏是等价的。

#### 二、采购时的验收标准（比原表述更严）

向供应商确认「支持多 PIN」不足以判定可用，必须逐条落实：

| # | 必须确认项 | 不满足的后果 |
|---|---|---|
| 1 | PIN-B 认证下，PIN-A 的存储区是**不可见**（枚举不到 DF/EF、看不到尺寸）还是仅**不可读**？ | 仅不可读 → 存在性泄露，等于没做 |
| 2 | 是否存在全局 SO PIN / 管理员 PIN 能解锁或重置全部区域？ | 存在 → 对手直接索要该 PIN，防线归零 |
| 3 | PIN-A 的重试计数器 / 锁定状态能否在 PIN-B 会话下被观测？ | 能 → 存在性泄露 |

**经验判断**：通用 PKCS#11 token 基本过不了第 1 条（卡 OS 通常暴露文件系统结构）。
真要满足需 Java Card 定制 applet（飞天、握奇、龙脉等可做），采购周期与成本再涨一档。

#### 三、连锁影响：若卡不支持，三条已拍板的结论要重开

| 已定事项 | 若卡不支持多 PIN |
|---|---|
| **Q-01 保隐藏** | 前提不成立，需重新向客户确认。三模式设计（含时间戳告警、诱饵预置、文档明示三项硬性需求）失去意义 |
| **Q-02 卡丢=数据全没** | 这个代价是**为换否认性才付的**。否认性没了即为净亏，客户大概率会要求补恢复码 |
| **《基本需求》第 7 条** | "三个密码都存卡里"与否认性直接冲突，条目本身需修改 |
| **R-01 模式 B** | `bProtectHiddenVolume=FALSE` 带来的"真实数据可能被覆盖"风险照旧存在，却换不来任何否认性 |

#### 四、✅ 已采纳方案：把否认性押在密钥派生上，而非硬件上

> **客户已于 2026-08-17 通过 Q-06 选 A 确认采用本方案。以下为最终设计，非备选。**

卡只存**一个不透明的密钥块 S**（看不出用途、看不出数量）：

- 外层卷密码 = `KDF(S, "outer")` —— 软件自动填入，用户无感
- 隐藏卷密码 = `KDF(S, 用户口令)` —— 口令只存在于用户记忆中，**从不落卡**

胁迫时：交卡、交 PIN，软件用 S 自动挂出外层卷，全程自然。
**卡上没有任何东西暗示存在第二个卷** —— 因为确实只有一个 blob，多 PIN 与否完全无关。

| | 评估 |
|---|---|
| ✅ | 对卡的要求降到"能存 32 字节 + 一个 PIN"，任何普通安全卡满足，**采购风险归零** |
| ✅ | 比多 PIN 卡更强：不依赖供应商对"不可见"的实现质量，也不惧侧信道 / 芯片分析 |
| ✅ | 与 Q-02 兼容：卡丢了照样全没，语义不变 |
| ⚠️ | 用户需记**一个**口令，与"用户无需记忆密码"有出入 → **客户已接受，见 Q-06** |
| ⚠️ | 该口令是唯一的抗离线爆破屏障，KDF 须选 Argon2id 并按高成本参数配置（Q-06 硬性需求 1） |
| ⚠️ | 模式 A 需输口令、模式 B 不需要，**UI 入口必须完全一致**，否则界面本身泄露隐藏卷存在（Q-06 硬性需求 4） |

**不推荐的两条路**：
- **双卡（一张胁迫卡、一张真卡）** —— 藏一个实体比藏一句口令难得多，且与 Q-02"不做备用卡"的精神冲突。
- **换供应商定制 applet** —— 能解决，但采购 + applet 开发是最长的关键路径，且仍需验证"不可见"。

#### 五、对 P1 的接口要求（**已修订，与原表述相反**）

~~原：P1 设计卡通信封装时必须把"多 PIN + 分区权限"作为接口假设预留。~~

**现改为**：把 `ICardStore` 抽象成**单 blob 的 get/put + 一个 `capabilities` 能力位**，
密钥派生逻辑放在其上层。

如此一来，多 PIN 从"必要条件"降级为"可选增强"——
硬件到货后支持则多一层纵深防御，不支持也无需改动任何设计。

**若仍按多 PIN 假设开发**，等硬件到 P2/P3 才发现不支持，需返工卡通信封装、
建卷向导、登录流程与全部文档，**预估 1-2 周 + 一轮客户重新确认**。

> **⏸ 2026-08-16 状态：客户决定硬件采购暂缓，此项挂起。**
> **✅ 2026-08-17 结案：客户经 Q-06 选 A，采用第四节方案，本项不再是 Q-01 路线的阻塞项。**
> Spike 4 仍建议在硬件到货后执行，但其结论只影响"是否追加纵深防御"，
> 不再影响 Q-01 是否成立。采购可按普通安全卡（能存 32 字节 + 单 PIN）询价，
> **多 PIN 不再是选型门槛**。

### N-03 ✅ 备份"明文拷贝"与产品安全定位冲突（**2026-08-19 已由 Q-04 结案**）

> **结论：定为明文导出，用户自担风险。** 冲突不消除，改为由用户知情承担。
> 完整决策、落地要求与 F-037 的处置见**第四节 Q-04**。以下保留原分析供留档。
>
> **✅ 2026-08-19 措辞歧义已澄清（Q-05 答复 2）：确为 (a) 导出解密后的原始文件。**
> 即**本项冲突是真实的，不是措辞误解** —— 备份文件落地后确实完全不加密。
> 此前设想的"若为 (b) 则冲突不存在"的退路不成立。
> 客户已知情并选择承担，见 **Q-04**。

《基本需求》第 5 条"拷贝备份数据明文拷贝出来，用户自己决定备份存在哪"，
意味着备份文件落地后**完全不加密**。

这与原需求书 F-037"备份数据完全随机无特征"**直接矛盾**，
也与整个产品的安全定位冲突。

~~**建议**：至少提供"明文导出 / 加密备份"两个选项，由用户选择。需客户确认（Q-02，编号笔误，应为 Q-04）。~~

**➡️ 2026-08-19：已由 Q-04 拍板，选择"仅明文导出、给足自由度、用户自担风险"。以第四节 Q-04 为准。**

### N-04 🟡 卷路径格式（技术细节，已解决但需记录）

传给驱动的卷路径**必须原样传递，不能加 `\??\` 前缀**。

依据：官方 `CreateFullVolumePath()`（`Common/Dlgcode.c:1568`）仅原样复制路径，
只用开头是否为 `\DEVICE` 判定裸设备。路径转换由驱动内部完成。
加前缀会导致 `ERROR_PATH_NOT_FOUND(3)`。

此点已在 `spike/vcspike.c` 中修正并注明依据，供 P1 实现时参照。

### N-05 🔴 "加密盘"不能是外层卷 —— 必须改为双容器架构（2026-08-17 新增）

**这是本轮最重要的发现，此前所有版本均未识别。**

#### 问题

**核心理由（与时序无关）**：VeraCrypt 的外层卷是**牺牲品**。隐藏卷的字节就落在外层卷
文件系统眼中的"空闲空间"里，而该文件系统**完全不知道那片区域被占用**。
按 Q-01 已拍板的"模式 B 不开保护"，往外层卷写入的数据会静默覆盖隐藏卷 ——
**这是持久的、跨会话的破坏**：今天挂外层写 10GB，下周再挂隐藏盘，数据一样已经没了。

**即：外层卷在任何时刻都不能承载真实数据，与是否同时挂载无关。**
此前"一个容器 = 外层卷(加密盘) + 隐藏卷(隐藏盘)"的隐含假设，从一开始就没对上需求。

**辅助依据（同时挂载）**，《基本需求》原文：

> 第 1 条：插上 USB 卡，应用软件打开登录成功自动加载已经创建的加密盘**与**隐藏盘
> 第 3 条：拔掉 USB 卡…自动卸载加密盘**以及**隐藏盘，盘符不可见

> **⚠️ 措辞订正（2026-08-17）**：本节初稿曾援引第 6 条"用户可以在加密盘**或者**隐藏盘中
> 新建、编辑、删除文件"来论证"同时读写"，**属过度解读**——该条用的是"或者"，
> 且重点在"与正常文件操作一致"（指透明加解密），不能用作同时性依据。已删除该论据。
>
> **且第 1、3 条的"与/以及"是否真要求两个盘符同时在线，本身存在歧义**
> （也可能只是"该加载的都加载上"的并列表述）。**已并入 Q-05 请客户澄清。**
> 但**即便客户澄清为"不要求同时"，本节结论依然成立**——理由是上方的核心理由，不是同时性。

#### 为何不能"同一容器同时挂外层 + 隐藏"

曾考虑：正常登录时把外层与隐藏一起挂出两个盘符。**不可行**，三条理由：

1. VeraCrypt 不允许同一容器挂载两次（独占打开）。*注：此点未实测，但被第 2 条覆盖。*
2. 🔴 **物理上必然互相破坏** —— 隐藏卷就住在外层卷的"空闲空间"里。
   两个文件系统同时读写挂载，外层卷的 NTFS 会把簇分配到隐藏卷所在区域。
   **不是有风险，是必然踩踏。** 开保护可防，但 R-01 已论证开保护会暴露隐藏卷存在。
3. 两个盘符同时出现在资源管理器中，否认性当场归零。

#### ✅ 解法：双容器架构

| | 形态 | 用途 | 写入安全性 |
|---|---|---|---|
| **容器 A** | 普通卷（独立文件） | **加密盘** —— 日常真实使用 | ✅ 完全安全，无覆盖风险 |
| **容器 B** | 外层卷 + 隐藏卷 | 外层 = 诱饵"隐藏盘"；隐藏卷 = **真·隐藏盘** | 外层写入仍可能摧毁隐藏卷（Q-01 已接受） |

登录流程：

| 输入 | 挂载结果 | 对手视角 |
|---|---|---|
| **真实密码** | 容器 A（加密盘）+ 容器 B 的**隐藏卷**（隐藏盘） | —— |
| **胁迫密码** | 容器 A（加密盘）+ 容器 B 的**外层卷**（诱饵隐藏盘） | 看到"加密盘 + 隐藏盘"两个盘，与产品宣称完全一致 |

两条路径的**盘符数量、界面、耗时、流程完全一致**，无任何可观察差异。

#### 该架构的两个额外收益

1. **否认策略由"否认存在"升级为"展示诱饵"**：产品功能列表本就明写有隐藏盘，
   想否认其存在本无胜算。改为大方挂出一个装满诱饵的"隐藏盘"后，
   对手的核对清单被完整满足，**没有继续追问的抓手**。这比原方案强得多。
2. **Q-01 接受的数据损失风险大幅缩小**：容器 A（日常数据）彻底安全，
   "真实数据可能永久丢失"从波及全部数据缩小为**仅波及隐藏盘内容**。

#### 代价与前置条件

- 用户需记**两个**密码：真实密码 + 交出去的胁迫密码（原 Q-06 只需记一个）
- 🔴 **必须手动输入密码**，否则无法表达"我正在被胁迫" —— 见 A-02 与 **Q-07**

> **✅ 2026-08-17 订正**：本节初稿曾把"建卷向导需**一次创建两个容器**、流程变长"列为代价，
> **该耦合无任何技术依据，已取消**。两个容器之间零依赖：
> 加密盘容器就是一个普通卷；隐藏盘容器的"建外层 → 填诱饵 → 扫簇位图 → 建隐藏卷"
> 全程自包含，不需要知道加密盘是否存在。
>
> **改为与 VeraCrypt 一致：一次建一个容器，分两次独立完成。** 好处：
> 1. **隐藏盘天然成为可选项** —— 比在向导里加档位开关自然得多（见 8.3）
> 2. **可后补** —— 用了半年才决定要隐藏盘，无需重建加密盘
> 3. 符合 VeraCrypt 的心智模型，用户文档与支持成本更低
> 4. **Spike 3b 的验收标准本就是单容器的**，无需为"两阶段编排"再加一层
>
> ⚠️ **注意区分**：建卷分两次 **≠** 挂载分两次。登录后仍需各挂一次，
> 故 **Spike 6 的五秒预算问题不受影响**。建卷是一次性低频操作，挂载是每次登录的必经路径。

#### 技术附注：容器格式与外层卷的安全边界（源码依据）

**（1）一个容器最多两个卷，格式写死**

`Common/Volumes.h`：

| 常量 | 值 | 行 |
|---|---|---|
| `TC_VOLUME_HEADER_SIZE` | 64KB | :51 |
| `TC_VOLUME_HEADER_OFFSET` | 0（标准/外层卷头） | :60 |
| `TC_HIDDEN_VOLUME_HEADER_OFFSET` | 64KB（隐藏卷头） | :61 |
| `TC_VOLUME_HEADER_GROUP_SIZE` | 2 × 64KB（开头两个槽位） | :56 |
| `TC_TOTAL_VOLUME_HEADERS_SIZE` | 4 × 64KB（另加末尾两个备份头） | :57 |
| `TC_VOLUME_DATA_OFFSET` | 128KB | :77 |

**只有 2 个卷头槽位，没有第三个。** 推论：

- **一个容器不能有两个隐藏卷** → 这闭环了 v1 清单的 **G-03**
  （"F-045 暗示可能支持多个隐藏卷"）：**VeraCrypt 原生做不到，多个隐藏盘只能靠多个容器。**
- **普通卷与外层卷在磁盘上结构完全相同**，无任何标志位区分。
  唯一差异是偏移 64KB 的槽位里是有效头还是随机数，而该数据本身加密，
  不知道密码无法分辨 —— **这是否认性的物理基础**（另见第八节）。

**（2）隐藏卷的物理位置**

`Common/Format.c:131`：

```c
dataOffset = volParams->hiddenVolHostSize - TC_VOLUME_HEADER_GROUP_SIZE - volParams->size;
```

隐藏卷占据 `[hostSize − 128KB − hiddenSize, hostSize − 128KB)`，即紧贴末尾备份头之前的**尾部区域**。

**（3）🔴 "外层占用 < 总量 − 隐藏卷"不是安全保证**

该推论方向正确但**只是必要条件，不是充分条件**，三点依据：

- **边界基准是建卷瞬间的簇位图快照，不是简单减法。** `Tcformat.c:9884` 注释：
  扫描求的是 *"the size of the uninterrupted block of free space **whose end is aligned with
  the end of the volume**"*。外层卷若已有碎片，可用尾块会小于"总量 − 已用"。
- **VeraCrypt 自留余量仅 0.5%、封顶 10MB**，且官方注释自承是防意外用的
  （`Tcformat.c:9758-9763`）：

  > `// Add a reserve (in case the user mounts the outer volume and creates new files`
  > `// on it by accident or OS writes some new data behind his or her back, such as`
  > `// System Restore etc.)`

- 🔴 **决定性：外层卷的文件系统根本不知道尾部被占用。** 那片区域在 NTFS 位图里就是"空闲"，
  **无任何预留机制**。NTFS 不承诺从前往后分配 —— MFT 区扩张、`$LogFile`、碎片整理、
  卷影副本/系统还原、索引服务都可能落到尾部。
  （官方另一处佐证：`OUTER_VOLUME_TOO_SMALL_FOR_HIDDEN_OS_NTFS` 提到
  *"NTFS file system always stores internal data exactly in the middle of the volume"*。）

**结论：不开保护时，对外层卷的任何写入都是不安全的，且超线时无任何报错。**
安全地修改外层卷只有一条路 —— 开启隐藏卷保护，见**第八节**。

**（4）容器一旦建成，尺寸锁死**

- VeraCrypt **无缩容功能**（闭环 v1 清单的 **G-01**）
- 扩容工具 `VeraCryptExpander` **明确拒绝**含隐藏卷的容器。
  `ExpandVolume/WinMain.cpp:62-63`：
  > *Do not use this software to expand an outer volume containing a hidden volume,
  > **because this destroys the hidden volume!***

  `DlgExpandVolume.cpp:572-577` 会先询问再拦截。

**产品影响**：容器 A/B 的容量与隐藏卷尺寸在建卷时一次定死，事后不可调整。
**建卷向导必须让用户充分理解这一点**，容量规划失误只能重建 + 迁移数据。

### N-06 🔴 档一（默认档）没有认证锚点，登录密码形同虚设（2026-08-19 新增）

#### 问题

Q-07 确定「容器 B 挂载成功即认证成功，**无需单独的登录密码校验环节**」。
**但 8.3 的档一只建了加密盘容器，根本没有容器 B。**

而按 Q-07 的订正，容器 A 的密码 = `KDF(S, "diskA")`，**只从卡派生、与用户输入无关**。
两者相加的后果是：

> **档一用户在密码框里输入任何内容都能登录成功。**

且**档一是默认档位**，即该缺陷影响的是大多数用户。

同时它与两条已定需求冲突：《基本需求》第 7 条预设存在"登录密码"，
Q-02 的"忘记密码 = 登录密码重置"也预设登录密码是有意义的。

#### 为什么容器 A 必须从卡派生（根因，勿再尝试推翻）

容器 A 在模式 A 与模式 B 下**都必须挂出**（否则胁迫时只出现 1 个盘符，
与产品宣称的"加密盘 + 隐藏盘"不符，当场穿帮）。
但两种模式用户输入不同，`KDF(S, P_real) ≠ KDF(S, P_duress)`，
而 **VeraCrypt 标准卷的卷头只有一个、只匹配一个密码** ——
若 A 绑定用户输入，必然在其中一种模式下打不开。这正是 Q-07 那条订正的由来。

> **不可行的变通**：把容器 A 也做成"外层 + 隐藏"的双卷容器以支持两个密码 ——
> 这会使 A 退化为牺牲品，日常写入不再安全，正是 N-05 明令禁止的形态。

#### ✅ 解法

**（1）「输错密码则两个盘都不挂」的效果由挂载顺序保证，不需要改派生**

```
1. 先挂容器 B（密码 = KDF(S, 用户输入)）
2. B 失败 → 立即返回认证失败，容器 A 根本不发起挂载
3. B 成功 → 才挂容器 A
```

对用户与对手而言，行为与"A 也校验密码"完全一致。
🔴 **顺序不可颠倒**：容器 A 输什么密码都挂得上，若先挂 A，
错误密码下会闪出一个盘符再消失 —— 既是逻辑错误，也是可观察的异常。
Spike 6 已证明并行挂载无效（驱动路径串行化），故强制串行**不产生任何性能代价**。

**（2）档一改为容器 A 绑定用户输入**

档一**没有胁迫模式**（无隐藏盘可藏，隐藏盘容器文件根本不存在），
即档一下**只有一个正确密码**，上述"标准卷只认一个密码"的限制在该档不构成问题。

| 档位 | 容器 A 的密码 | 认证锚点 |
|---|---|---|
| **一** | `KDF(S, 用户输入)` | 容器 A 自身 |
| **二 / 三** | `KDF(S, "diskA")` | 容器 B（由上述挂载顺序 gate） |

**（3）升档时改写容器 A 的卷密码**

用户从档一升到档二/三时，需把 A 的密码由 `KDF(S,P)` 改为 `KDF(S,"diskA")`。

**该能力现成、成本极低**：`Common/Password.c:190` 的
`int ChangePwd (const wchar_t *lpszVolume, Password *oldPassword, int old_pkcs5, int old_pim, Password *newPassword, int pkcs5, int pim, int wipePassCount, HWND hwndDlg)`
非 `static`，且 `Password.obj` 已存在于 `FormatDLL/x64/Release/` ——
**我方要 fork 的 FormatDLL 工程本来就编译了它，可直接调用。**

> **✅ 2026-08-19 已实测通过**，见 `spike/SPIKE3B-建卷工具编译验证.md`「附加验证」一节。
> 给 FormatDLL 加一个 `VeraCryptFormat_ChangePassword` 导出即可，
> 实测：改密码返回 `VCF_SUCCESS`、**全程零弹窗**、新密码可挂载、旧密码被正确拒绝。
>
> 🔴 **保持无界面需要两件事，缺一不可**：
> 1. `Silent = TRUE`（`VeraCryptFormat_Initialize()` 已设）→ `handleError()` 直接返回
> 2. **`SetRandomPoolEnrichedByUserStatus(TRUE)`** —— **`Silent` 覆盖不到这条**。
>    `ChangePwd` 会调 `UserEnrichRandomPool()`（`Dlgcode.c:7421`），
>    它用 `DialogBoxParamW` 弹"晃鼠标收集熵"对话框，只看
>    `IsRandomPoolEnrichedByUser()`，**不看 `Silent`**
>
> **两点须记入产品实现**：
> - **耗时约 2.8 秒**，升档时 UI 需给进度反馈，否则看起来像卡死
> - 跳过鼠标熵采集是**有意识的取舍**（与官方 FormatDLL 建卷路径行为一致，
>   它压根不调用该采集），**应在安全设计文档中留档**

#### 已评估并否决的两个替代方案（留档）

| 方案 | 否决理由 |
|---|---|
| 取消档一，默认就建两个容器 | 🔴 **直接违反 8.3** —— 人人都有诱饵结构，对手确知必有隐藏层，**否认性归零**。以核心价值换实现便利，不可取 |
| 档一不显示密码框，纯卡认证 | 逻辑上诚实（档一的安全边界本就是卡，符合第 8 条），但与第 7 条、Q-02 冲突，会引出新的需求变更 |

**不可区分性影响：无。** 8.4 只要求**档二与档三**不可区分；
档一的隐藏盘容器文件根本不存在，本就自证，其内部派生方式不同不构成任何泄露。

### N-07 🔴 Q-01 硬性需求 1（模式 B 写隐藏卷时间戳）在现行设计下不可实现（2026-08-19 新增）

#### 问题

Q-01（2026-08-16）要求：「模式 B 触发后，在**隐藏卷内部**写入时间戳，下次正常登录时告警」。

**该条已被 Q-06/Q-07（2026-08-17）的密钥派生设计作废。**
模式 B 下软件手中只有 `KDF(S, P_duress)` = 外层卷密码，
按 8.2 的设计性质「**根本算不出隐藏卷密码…不是拒绝执行，是物理上不持有该数据**」——
**算不出密码，自然也写不进去。** 这是同一件事的两面，无法同时要两者。

#### 该需求原本要防的场景（不可放弃，必须替代实现）

| 场景 | 用户需要知道什么 |
|---|---|
| **明着胁迫** | 用户知道发生了，但**不知道对手往外层卷写了多少** —— 模式 B 不开保护，覆写隐藏卷时**无任何报错** |
| **偷着用卡**（更关键） | 卡被短暂拿走用过又放回，**用户完全不知情**，会继续把已污染的容器当安全的用 |

**若无告警，Q-01 所接受的"数据可能永久丢失"就退化为"丢了都不知道"，性质完全不同。**

#### 换个位置写也都不行

| 写入位置 | 否决理由 |
|---|---|
| 外层卷 | 对手正在看这个盘，直接可见 |
| 卡上加"胁迫时间"字段 | 违反 N-02 的单 blob 原则；且对手持有 PIN，读得到 |
| 本地配置 / 日志 | 档二用户没有胁迫模式，该字段本身即档三标记 → **违反 8.4** |

#### ✅ 替代方案：拆成"检测后果"与"检测事件"两个机制

**（1）检测后果 —— 隐藏卷内维护完整性校验**

隐藏卷内存一份文件校验清单，**模式 A 登录时核对**，发现覆写立即告警。

- 技术可行：模式 A 下持有隐藏卷密码
- **告知用户的信息比时间戳更有用** —— 用户真正关心的是"数据坏没坏"，
  而非"外层卷被挂过"

**（2）检测事件 —— 卡上单调计数器**

```
卡上：      C = 挂载成功次数（模式无关，每次 +1）
隐藏卷内：  上次模式 A 登录时记录的 C 值

模式 A 登录时：
  若 (卡上 C) − (隐藏卷记录的 C) > 1     ← 1 = 本次自身那一次
  → 中间发生过非模式 A 的挂载 → 告警
```

**为何不违反否认性**：计数器对模式与档位**完全不敏感**，
对手看到"挂载次数 = 37"推不出任何东西。它不是密钥材料，是一个公开的使用计数，
不违反 N-02 单 blob 的精神，也不构成 8.4 意义上的档位痕迹。

> ⚠️ 模式 C 同样会使 C 递增，故**模式 C 结束时须一并更新隐藏卷内的记录值**，否则误报。

两者互补：(1) 抓"数据已被写坏"，(2) 抓"卡被人用过但没写"。

#### 🔴 已知局限（必须写入文档，不得当作万无一失的保证）

对手若直接读出 S、用自研工具挂载卷，**不经过我方软件**，
则计数器不递增、校验记录不更新，**上述两个机制均检测不到**。
这是任何基于自身软件的检测机制的共同上界。
（对手若专业至此，整套方案本也无法阻拦，但用户须知情。）

#### 🔴 对 USB 卡采购的新增要求

本方案要求卡**支持可写的持久计数器**。
当前对卡的要求是"能存 32 字节 + 一个 PIN"（N-02 第四节），
**写能力须在采购询价时一并确认** —— 这是继"多 PIN 降级为可选"之后
**对卡的第二项硬性要求，勿遗漏**。

> 若卡不支持可写计数器：机制 (1) 完整性校验仍可独立成立，
> 仅失去"卡被用过但没写"这一类的检测能力，属可接受的降级。

### N-08 🔴 建卷需要管理员权限，且提权行为本身会泄露档位（2026-08-19 新增）

> **来源**：Spike 3b 里程碑 2 实测 + `spike/probe_bitmap.c` 探针。
> 完整数据见 `spike/SPIKE3B-建卷工具编译验证.md`。

#### 问题

计算隐藏卷最大尺寸必须扫描外层卷的簇位图
（官方 `ScanVolClusterBitmap()`，`Format/Tcformat.c:9997`）。
探针实测，两种文件系统 × 两种权限，**共四组**：

| 打开方式 | 普通用户 | **管理员** |
|---|---|---|
| `GENERIC_READ`（官方用法） | CreateFile **err=5** | ✅ **OK**（FAT32 与 NTFS 均成功） |
| `FILE_READ_DATA \| SYNCHRONIZE` | CreateFile **err=5** | ✅ **OK** |
| `FILE_READ_ATTRIBUTES \| SYNCHRONIZE` | FSCTL **err=1** | ❌ **仍然 err=1** |
| `0` | FSCTL **err=1** | ❌ **仍然 err=1** |

**关键在后两行：提权后依然 `err=1`。** 说明 `err=1` 与文件系统无关、与权限也无关，
真正含义是**该 FSCTL 要求句柄具备 `FILE_READ_DATA`**；
而带读数据权限打开卷设备需要管理员。因果链确定且**无绕路余地**：

```
FSCTL 要 FILE_READ_DATA  ->  带 FILE_READ_DATA 开卷要管理员  ->  必须提权
```

#### 修正后的权限矩阵

| 路径 | 频率 | 管理员权限 |
|---|---|---|
| 挂载 / 卸载 / 查询（日常登录、拔卡卸载） | 每次登录 | ✅ **不需要**，R-02 成立 |
| 建**普通卷**（加密盘容器、档二的隐藏盘容器） | 一次性 | ✅ 不需要（无需扫位图） |
| 建**隐藏卷**（档三） | 一次性 | 🔴 **需要** |
| 模式 C"剩余可写空间"显示（8.5） | 低频但**非一次性** | 🔴 **需要**（同一算法） |
| Q-01 硬性需求 2 的诱饵阈值校验 | 建卷时 | 🔴 **需要**（同一算法） |

#### 🔴 连带的否认性问题

按上表，**档二建卷不提权、档三建卷提权** ——
**UAC 提示本身就区分了两个档位**，且 Windows 会记录提权事件。
**直接违反 8.4 的本地状态层不可区分要求。**

**✅ 缓解（低成本，必须做）**：**建卷流程无条件提权，与档位无关。**
两档表现完全一致，差异消失。代价仅是档一/档二多弹一次 UAC，可接受。

#### ✅ 模式 C 必须提权（原为待决策，2026-08-19 由碎片化测试定案）

**碎片化测试**（`spike/frag_test.cpp`）：建 100MB 卷 → 顺序写 5×10MB →
删掉中间的 f1、f3 制造空洞 → 三个时点各扫一次簇位图：

| 阶段 | 空闲段数 | 尾部连续 | 空闲总数 | 朴素估算高估 |
|---|---|---|---|---|
| A 空卷 | 1 | 98.95 MB | 98.95 MB | 0.0% |
| B 顺序写满 | 1 | 48.95 MB | 48.95 MB | 0.0% |
| **C 删中间两个** | **3** | **48.95 MB** | 68.95 MB | 🔴 **20.00 MB / 40.9%** |

**由此得出的关键分布**：

- **建卷时碎片风险很低** —— 流程恰好是"新格式化的外层卷 + 用户顺序拷入诱饵"，
  即上表 B 形态。这解释了 Spike 3b 阶段 2b 为何实测 0% 差距
- 🔴 **模式 C 时碎片风险很高** —— 用户过几个月更新一次诱饵、加加删删，
  碎片持续累积，逐步向 C 形态演变。而 8.5 的"显示还可添加 X MB"用的是同一套算法

**这把"模式 C 要不要提权"从两难变成单选**：
保守估算方案不可取 —— **模式 C 恰恰是碎片最严重的场景，
即估算错得最多的时候，恰好是后果最严重的时候**（算多了就写坏隐藏卷）。

➡️ **定案：模式 C 照常提权。** 该功能本就要求"独处时使用"，多一次 UAC 不构成额外暴露。

> **附带**：该测试同时排除了"扫描器根本没在算连续"的可能 ——
> 空闲段数 1→1→3 与空洞数吻合、差值 20.00 MB 正好等于删掉的两个文件、
> 尾部连续值在 B→C 间纹丝不动。**扫描器正确性已确证。**

---

## 四、客户确认结果

### Q-01 ✅ 已确认（2026-08-16）｜取向：**保隐藏**

客户在"保数据 / 保隐藏"二选一中选择 **保隐藏**，并接受其代价。

**由此确定**：R-01 的三模式设计正式生效。模式 B（胁迫交出）**必须** `bProtectHiddenVolume = FALSE`。
对手若向外层卷写入数据，隐藏卷被静默覆盖、真实数据永久丢失 —— 此代价客户已知情接受。

**随之成为硬性需求的三项**（原为"建议的缓解措施"，现升级为必做）：
1. ~~模式 B 触发后在**隐藏卷内部**写入时间戳，下次正常登录时告警~~

   > 🔴 **2026-08-19 实现路径作废，改由 N-07 替代 —— 但告警这项需求本身保留且仍为硬性。**
   > 原因：Q-06/Q-07 的密钥派生设计使模式 B **物理上不持有隐藏卷密码**（见 8.2），
   > 因而无法向隐藏卷写入任何内容。
   > **新实现**：隐藏卷内维护完整性校验（检测数据是否被覆写）
   > \+ 卡上单调计数器（检测卡是否被非模式 A 使用过）。详见 **N-07**。
2. 建卷向导必须引导用户在外层卷预置足量诱饵数据 —— 诱饵越充实，对手越没有动机往里写

   > **✅ 2026-08-17 量化（原为无法验收的定性表述）**：诱饵占用应逼近
   > `可用尾部连续空闲块 − 隐藏卷尺寸 − VeraCrypt 自留余量`。
   > 该值即建卷时 `DetermineMaxHiddenVolSize()` 用到的同一组数据，我方可直接算出。
   >
   > **物理意义**：这个差值就是"胁迫时对手最多能写进去多少字节而不触及隐藏卷"。
   > 填得越满，对手写入导致覆盖的概率越低。
   >
   > **落地为建卷向导的硬校验**：诱饵占用未达设定阈值即不允许完成建卷（阈值待定，建议 ≥80%）。
   > 同一算法复用于模式 C 的"剩余可写空间"显示，见第八节。
3. 产品文档与首次使用引导中明示"胁迫模式下真实数据可能永久丢失"

**✅ 前提依赖已闭合（2026-08-17）**：本结论成立的前提是「胁迫 PIN 下拿不到隐藏卷密码」。
该前提原本挂在 USB 卡的多 PIN 能力上，现由 N-02 第四节的密钥派生方案保证，
**客户已于同日通过 Q-06 选 A 确认采用该方案**。Q-01 不再受硬件采购阻塞。

**✅ 流程依赖亦已闭合（2026-08-17）**：另一前提是「用户有操作空间只交出诱饵」，
该前提与《基本需求》第 1 条的全自动挂载冲突（见 A-02），
**客户已通过 Q-07 拍板改为手动输入密码**，此依赖闭合。

**✅ 代价已缩小（2026-08-17）**：经 N-05 双容器架构，上述"真实数据永久丢失"的风险
**仅波及隐藏盘**；加密盘（独立普通卷）的日常数据完全安全，不受模式 B 影响。
上列硬性需求第 2 项"预置诱饵"的对象明确为**容器 B 的外层卷**。

### Q-02 ✅ 已确认（2026-08-16）｜**接受"卡丢 = 数据全没"**

客户选择不做任何逃生通道：不提供备用卡，不提供恢复码。

**澄清的语义**：《基本需求》第 7 条的"忘记密码"仅指**登录密码重置**（凭卡的持有验证即可）。
加密盘/隐藏盘密码由软件从卡中读出自动填入，用户本就无需记忆，不存在"忘记"场景。
真正的风险是**卡丢失/损坏 → 容器永久无法打开**，客户已接受。

**未选恢复码的理由（留档）**：恢复码等价于密码本身，对手取得即可打开隐藏盘，
会直接拆掉 Q-01 所选的否认性设计。二者不可兼得。

**由此产生的可验收项**：
1. 🔴 建卷流程中必须有**不可跳过的风险确认**（明示"卡丢失即数据永久无法恢复"，用户主动勾选后方可继续）——
   这是客户接受该风险的落地形式，也是纠纷时的举证依据
2. 原 **T-013"提示联系管理员恢复"→ 删除**。消费级个人产品无管理员角色（见 Q-03），此条无法落地
3. 卡的物理保管建议写入用户手册

### Q-03 ✅ 已确认（2026-08-16）｜目标客户：**个人用户**

不涉及企业与党政机关采购。

**影响（重大利好）**：中国《商用密码管理条例》的型号核准/检测认证要求不触发，
**不存在强制国密算法（SM2/SM3/SM4）的场景**。VeraCrypt 的 AES/Serpent/Twofish 可用，
**技术选型就此锁定，v1 中"整个选型可能需重做"的最高风险正式解除。**

连带结论：产品无管理员角色（见 Q-02 第 2 项）。

### Q-04 ✅ 已确认（2026-08-19）｜**明文导出，给足自由度，用户自担风险**

**决策**：不做加密备份选项，只提供明文导出。用户自行决定备份存放位置，
并自行承担备份落地后的全部风险。《基本需求》第 5 条按原文实施，不修改。

**定位**：与 Q-02"卡丢 = 数据全没"属同一类决策 —— **用自由度换责任转移**。

#### 已知并被接受的四项风险

1. 🔴 **可能击穿否认性（最严重）**：若用户导出的是**隐藏盘**内容，
   这份明文备份的存在本身即"隐藏盘存在"的物证。胁迫场景下对手搜到即**当场归零**，
   无需破解、无需逼问密码。
   > **注意其代价**：8.4 要求配置/注册表/日志/最近使用记录在档二档三之间**逐字节不可区分**，
   > P3 为此专排 2-3 天验收。用户一次明文导出即可使这一整套努力失效。
2. 🔴 **与 Q-02 的逻辑张力**：Q-02 拒绝恢复码的留档理由是"恢复码等价于密码本身"。
   而**明文备份比恢复码更弱** —— 恢复码尚需配合容器文件，明文备份直接就是数据本身。
   等于前门锁死、后门敞开。**此不一致客户已知情接受。**
3. 🔴 **安全强度塌缩至最弱副本**：容器加密、卡绑定、拔卡卸载、扫描随机数
   保护的都是容器。明文一旦落地到用户自选位置（U 盘 / 网盘 / NAS / 同步目录），
   攻击者可完全绕开本产品的全部机制。
4. 🔴 **删不干净**：普通删除只改文件表；SSD 上受 wear leveling 与 TRIM 影响，
   覆写擦除亦不保证有效。"备份完删掉"并不能真正删除，属**不可控的长期泄露面**。

#### 由此产生的可验收项

1. 🔴 **不可跳过的风险确认**，与 Q-02（丢卡）、Q-06（口令遗忘）合并为同一屏多项勾选。
   须明示："导出后的数据不再受本软件保护，且可能击穿隐藏盘的否认性"
2. 🔴 **导出隐藏盘内容时单独二次确认** —— 加密盘导出与隐藏盘导出的后果不在一个量级
3. 🔴 **导出动作不得留任何本地痕迹**：日志、最近使用记录、上次导出路径一律不写。
   > **此项不属于"用户自担风险"的范畴，是我方实现义务。**
   > 用户承担的是"备份被人找到"的风险，**不是**"软件替他记录了他备份过隐藏盘"的风险。
   > 仍完整受 **8.4 本地状态层**约束，纳入 P3 的逐字节比对验收范围。
4. 用户手册明示残留问题（删除不擦内容、SSD 上覆写不保证有效）

#### F-037 的处置

原需求书 F-037"备份数据完全随机无特征"与本决策**直接矛盾且不可调和**，
按本决策 **F-037 删除**（第五节条目表已同步）。

#### ✅ 前提已坐实（2026-08-19，随 Q-05 一并确认）

第 5 条"明文拷贝出来"的措辞歧义**已澄清为 (a) 导出解密后的原始文件**（Q-05 答复 2），
且客户**再次确认** Q-04 的决策。

由此：**上述四项风险与四条可验收项全部生效，无一可省。**
此前"若澄清为 (b) 则大部分风险自动消失"的退路**不存在了**。

> **留档提醒**：Q-01 / Q-02 / Q-03 / Q-05 / Q-06 / Q-07 均为客户拍板并留档。
> **Q-04 正因属"用户自担风险"类型，出事时需要举证依据**，
> 建议与前述各项同等对待，取得**书面**确认后归档
> （本次为口头/即时确认，仍建议补书面件）。

### Q-05 ✅ 已确认（2026-08-19）｜范围维持《基本需求》9 条，两处歧义均已澄清

客户对本项的三个子问题一并答复：

| # | 问题 | 客户答复 |
|---|---|---|
| 1 | 是否以《基本需求》9 条为最终范围？ | ✅ **保持现状**，即以 9 条为准 |
| 2 | 第 5 条"明文"指 (a) 导出解密后的文件，还是 (b) 容器原样复制？ | ✅ **(a) 导出解密后的原始文件** |
| 3 | 第 1、3 条的"与/以及"是否要求两盘符同时在线？ | ✅ **不要求**，仅表示"该加载的都加载上" |

#### 答复 1 的意义：整个排期的地基就此坐实

以下三块内容确认**不在范围内**（原《需求说明书》有、《基本需求》无）：

- 隐藏分区模式一（改分区类型 ID）与模式二（UD 隐藏区，任意 LBA 偏移）
- 增量备份、脏块跟踪、断点续传、分卷存储
- **自研 KMDF 内核驱动 → 确认不需要 WHQL 认证**

**这三块正是原方案中周期最不可控的部分。** 由此正式确认：

- v1.0 采用**文件容器**形态（V-01 / V-02 降级为 v2.0 议题成立）
- **我方不编写任何内核驱动**，只需 EV 代码签名证书
- 3.5-4 个月的排期基线成立
- **第五节的删除建议就此可执行**（C-004、C-006、PK-008、T-01、T-013、F-037、F-045 等）

#### 答复 2 的意义：Q-04 的四项风险确认成立

第 5 条确为"导出解密后的原始文件"，**N-03 的冲突是真实的，不是措辞误解**。
Q-04 的四项风险与四条可验收项**全部生效**，无一可省。详见 Q-04。

#### 答复 3 的意义：无设计影响（如预期）

**方案不变。** N-05 双容器架构的核心理由是「外层卷是牺牲品，
任何时刻都不能承载真实数据」，**与同时性无关**，故该答复不触发任何改动。

> 🔴 **但有一处不得据此放松**：模式 A 与模式 B **必须挂出相同数量的盘符**。
> 这是**否认性要求**（8.2 / N-05 登录流程表），不是《基本需求》条文要求。
> 客户说"不要求同时在线"指的是需求条文的解读，
> **不意味着允许两种模式呈现不同的盘符数** —— 那会当场击穿否认性。
>
> 唯一的正面收益：为 v2.0 的形态调整留出了余地。

### Q-06 ✅ 已确认（2026-08-17）｜选 **A：隐藏卷密码不落卡，由用户口令参与派生**

**背景**：《基本需求》第 7 条要求隐藏盘密码存在卡中，但只要它以任何形式落卡，
否认性就取决于卡能否做到"胁迫 PIN 下不可见"——
这是采购周期最长、且供应商实现质量无法验证的一环（见 N-02 第一、二节）。

**客户选择 A**，即采用 N-02 第四节方案：卡只存一个不透明密钥块 S，
卷密码由 `KDF(S, 用户口令)` 派生，**口令仅存在于用户记忆中，从不落卡、从不落盘**。

> **⚠️ 2026-08-17 同日修订**：本条原写为"外层卷密码 = `KDF(S,"outer")` 由软件自动填入，
> 仅隐藏卷需要口令"。该写法在 N-05 双容器架构与 Q-07 手动输密码确认后**已作废**。
> **现行定义**：三个卷密码统一为 `KDF(S, 用户输入的口令)`，用户输入什么就派生什么、
> 挂出什么，软件不做任何自动填充。详见 Q-07。

**由此正式确定**：

1. **USB 卡采购彻底解除对 Q-01 的阻塞。** 对卡的要求降为"能存 32 字节 + 一个 PIN"，
   任何普通安全卡满足。多 PIN 从必要条件降级为可选纵深防御（Spike 4 非阻塞）。
2. **《基本需求》第 7 条措辞修改**（见第五节表格），新表述见下。
3. 否认性不再依赖任何硬件特性，也不惧对手对卡做侧信道 / 芯片分析 ——
   卡上确实只有一个 blob，物理上不存在"第二套密码"的痕迹。

**《基本需求》第 7 条建议新措辞**：~~见下~~ → **已由 Q-07 的版本取代，以 Q-07 为准。**

#### 随之成为硬性需求的四项

1. 🔴 **KDF 选型与参数**：口令是唯一的抗离线爆破屏障，必须用 **Argon2id**。

   > **✅ 2026-08-17 Spike 6 已实测定参**：推荐 **m=512 MiB, t=2, p=1**，
   > 保守（SSE2）路径约 **1.04 秒**，AVX2 路径 0.92 秒，符合 0.5-1 秒目标，
   > 且内存成本为原建议下限（256MB）的两倍。
   > **`p` 无效果** —— VeraCrypt 官方以 `ARGON2_NO_THREADS` 编译 Argon2，
   > 各 lane 串行执行，实测 p=1 与 p=4 无差异，取 1 即可。
   > 完整参数表见 `spike/SPIKE6-双容器挂载性能.md` 第四节。
2. 🔴 **建卷流程新增不可跳过的口令风险确认**：明示"口令遗忘 = 隐藏盘永久无法打开"，
   用户主动勾选后方可继续。形式与 Q-02 的丢卡风险确认一致，二者可合并为一屏两项。
3. 🔴 **口令强度校验**：建卷时对口令做最低强度约束并给出强度反馈。
   弱口令会直接抵消 Argon2id 的防护。
4. ~~🟡 **登录流程分叉**：正常使用（模式 A）需额外输入口令；模式 B 只需插卡 + PIN，
   不得有任何"还需要输入口令"的界面痕迹。~~

   **✅ 2026-08-17 已由 Q-07 的单密码框方案取代，该风险点消失。**
   现在两种模式都必须输入口令、界面完全相同，不存在"分叉"可供观察。
   原第 4 项从"我方 UI 实现的风险点"降级为"设计上已消除"。

**遗留风险（留档）**：口令遗忘 = 隐藏盘打不开，性质与 Q-02 已接受的"卡丢 = 数据全没"相同，
且同样不设恢复通道（设恢复通道即等价于把密码再存一份，会拆掉否认性）。

### Q-07 ✅ 已确认（2026-08-17）｜**用户手动输入密码**，放弃"插卡全自动挂载"

**背景**：A-02 指出全自动挂载与否认性不可兼得 —— 卡一交出软件即自动挂出真实隐藏盘，
用户没有任何操作空间表达"我正在被胁迫"。二者只能选一个。

**客户选择：手动输入密码。** 由此 Q-01 的保隐藏路线在流程层面正式闭环。

#### 最终登录设计

界面**永远只有一个密码框，永远必须输入**，不做任何自动填充、不做任何模式选择器。
输入什么就派生什么、挂出什么 —— 这正是 VeraCrypt 自身的语义
（密码先试标准卷头、不匹配再试隐藏卷头，Spike 2 已实测 `type=1/2`）。

**密码派生（2026-08-17 修订，见下方订正说明）**：

| 对象 | 密码来源 | 说明 |
|---|---|---|
| **容器 A**（加密盘） | `KDF(S, "diskA")` —— **仅依赖卡，与用户输入无关** | 两种模式下都要挂出，故不能绑定用户输入 |
| **容器 B** | `KDF(S, 用户输入的密码)` | 输入决定挂外层还是隐藏卷 |

> **⚠️ 2026-08-19 补充：本表仅适用于档二 / 档三。**
> **档一（只建了加密盘容器，无容器 B）下容器 A 改为 `KDF(S, 用户输入)`**，
> 否则该档没有任何东西校验用户输入，输任何密码均可登录。详见 **N-06**。

| 用户输入 | 挂载结果 |
|---|---|
| **真实密码** P_real | 容器 A（加密盘）+ 容器 B 的**隐藏卷**（隐藏盘） |
| **胁迫密码** P_duress | 容器 A（加密盘）+ 容器 B 的**外层卷**（诱饵隐藏盘） |
| 其它任意输入 | 容器 B 挂载失败 → **认证失败**，容器 A 亦不挂载 |

> **🔴 订正（2026-08-17）**：本节初稿写作"三个卷密码统一为 `KDF(S, 用户输入的口令)`"，
> **该写法有致命缺陷**：容器 A 在两种模式下都必须挂出，但两种模式用户输入不同、
> 派生结果不同，而 **VeraCrypt 标准卷只认一个密码** —— 容器 A 会在其中一种模式下打不开。
> 现改为容器 A 独立从卡派生。
>
> **否认性不受影响**：两种模式下用户都要输密码，输入只作用于容器 B，
> 对手无法察觉容器 A 其实没用到它。
>
> **附带好处**：《基本需求》第 7 条"加密盘密码保存在 USB 卡中"因此**真正成立**，
> 需改的只剩隐藏盘那半句。
>
> **登录校验副产品**：容器 B 挂载成功即认证成功，**无需单独的登录密码校验环节**。

**关键性质**：两条路径的界面、操作步骤、盘符数量、耗时完全一致。
**否认性不再依赖任何 UI 实现细节，而是设计上就不存在可观察的分叉。**

#### 《基本需求》需修改的条目（**以本节为准**）

| 条 | 原文 | 新措辞 |
|---|---|---|
| **第 1 条** | 插上 USB 卡，应用软件打开登录成功自动加载已经创建的加密盘与隐藏盘 | 插上 USB 卡并输入密码，登录成功后自动加载对应的加密盘与隐藏盘 |
| **第 7 条** | 登录密码、加密盘、隐藏盘密码都保存在 USB 卡中 | USB 卡中保存密钥材料；**加密盘密码由该密钥材料直接派生，用户无需记忆**；隐藏盘密码由该密钥材料与用户输入的登录密码共同派生，不以任何形式单独存储。用户需记忆日常登录密码与应急（胁迫）登录密码各一个 |

**第 8 条（未插卡应用无法打开）不受影响，保持不变。**

#### 随之确定的事项

0. 🔴 **挂载顺序强制为「先容器 B，后容器 A」**（2026-08-19 补充，见 N-06）。
   容器 A 的密码与用户输入无关，**输任何密码都挂得上**；若先挂 A，
   错误密码下会闪出一个盘符再消失 —— 既是逻辑错误，也是可观察的异常。
   本表"其它任意输入 → 容器 A 亦不挂载"正是由该顺序保证的。
   Spike 6 已证明并行挂载无效（驱动路径串行化），故强制串行**无任何性能代价**。
1. 🔴 用户需记忆**两个**密码：日常密码、胁迫密码。二者均须通过强度校验（Q-06 硬性需求 3）
2. 🔴 建卷向导需引导用户**分别设定**这两个密码，并明确解释各自用途与后果
3. 🔴 两个容器**独立创建、各建各的**（与 VeraCrypt 一致），**隐藏盘容器可后补**；
   建隐藏盘容器时引导用户在其外层卷预置诱饵。详见 N-05「代价与前置条件」的订正说明
4. ✅ P-002"认证到挂载 ≤5 秒"**已由 Spike 6 实测确认成立**：
   模式 A（Argon2id 1.04s + 容器 A 0.92s + 容器 B 隐藏卷 1.67s）= **3.63 秒，余量 1.37 秒**；
   模式 B 为 2.79 秒。

   > 🔴 **但有硬前提：挂载时必须显式指定 `pkcs5_prf`（建议 SHA-512），禁止用 0（自动探测）。**
   > 自动探测下隐藏卷单次挂载需 **6.6 秒**（Streebog 一项就占 9.2 秒），
   > 模式 A 端到端 8.9 秒，**超预算 79%**。
   > 由此产生两条落地要求：**建卷时锁定单一 PRF 并记录**、**挂载时显式传入**。
   >
   > ~~必要时两个容器的挂载并行化~~ —— **经实测无效，已作废**
   > （串行 2.61s vs 并行 2.55s，驱动挂载路径是串行化的）。
5. 🟡 原"插卡即自动登录"的便利性丧失，属客户已知情接受的代价

> **⚠️ 第 4 项是本次变更引入的新的排期/性能风险，请勿忽略。**
> 原 P-01 结论"性能余量充足（余量约 3.5 秒）"是按**单次挂载**算的，双容器后此结论需重算。

---

## 五、建议修改或删除的需求条目

| 编号 | 原内容 | 建议 | 理由 |
|---|---|---|---|
| C-004 | 支持 x86 32 位 | **删除** | SDK 无 32 位；Win11 无 32 位版 |
| C-005 | 基于 1.26.26 及以上 | **改为锁定单一版本** | SDK 正式 tag 仅到 1.26.24 |
| C-006 | 兼容 1.26.20 至最新 | **删除** | `MOUNT_STRUCT` 为二进制结构体，跨版本布局会变，强行兼容有数据风险 |
| C-007 | 修复 Win11 BSOD | **待测后删除** | 用官方签名驱动，交上游修复 |
| C-015 | 隐藏分区支持 NTFS/FAT32/exFAT | **重新表述** | v1.0 用文件容器，此条语义不明 |
| S-004 | 密钥不得明文驻留内存 | **改写** | 见 R-03 |
| F-046 | 通过 SDK 或源码 API 挂载 | **改为 IOCTL** | Mount SDK 不存在 |
| F-037 | 备份数据完全随机无特征 | **✅ 删除** | Q-04 已拍板选择明文导出，二者不可调和。见 N-03 / Q-04 |
| 基本需求-5 | 拷贝备份数据明文拷贝出来 | **保留原文不改** | Q-04：明文导出，用户自担风险 |
| PK-008 | 注册 Windows 硬件开发人员计划 | **删除** | 无自研驱动，不需要 |
| T-01 | 驱动代码单测覆盖率 ≥80% | **删除** | 无自研驱动 |
| T-013 | 提示联系管理员恢复 | **删除** | Q-03 确认为个人用户，消费级产品无管理员角色 |
| P-004 | 内存占用 ≤50MB | **保留，可达成** | 技术栈定为 C++ + WebView2，非 Electron |
| 基本需求-1 | 插卡后**自动**加载加密盘与隐藏盘 | **✅ 客户已确认修改**：改为"插卡并输入密码后"加载（措辞见 Q-07） | 全自动挂载下用户无操作空间表达胁迫，Q-01 落空。见 A-02 / Q-07 |
| 基本需求-7 | 登录/加密盘/隐藏盘密码**都**保存在 USB 卡中 | **✅ 客户已确认修改**：卡只存密钥材料，卷密码由其与用户输入的密码派生（措辞以 **Q-07** 为准） | 密码一旦落卡，否认性即取决于卡能否做到"胁迫 PIN 下不可见"，见 N-02 / Q-06 / Q-07 |
| —— | *新增*：加密盘与隐藏盘为**两个独立容器**，加密盘不得使用外层卷 | **新增需求** | 见 N-05。外层卷是牺牲品，不能承载日常数据 |
| —— | *新增*：建卷向导支持**分别创建**加密盘容器与隐藏盘容器，并在建后者时引导预置诱饵 | **新增需求** | Q-07 事项 3；两容器零依赖，不得耦合成一次流程 |
| —— | *新增*：登录界面恒为单一密码框，无自动填充、无模式选择器 | **新增需求** | Q-07；否认性由设计保证而非 UI 实现细节 |
| P-002 | 认证到挂载 ≤5 秒 | **保留，✅ Spike 6 已确认成立** | 模式 A 实测 3.63s，余量 1.37s。见 P-01 修订与 `spike/SPIKE6-双容器挂载性能.md` |
| —— | *新增*：**挂载必须显式指定 `pkcs5_prf`，禁止自动探测**；建卷时锁定单一 PRF 并记录 | **新增需求🔴** | Spike 6：自动探测下隐藏卷挂载 6.6s，模式 A 端到端 8.9s，超预算 79% |
| —— | *新增*：Argon2id 参数固定为 **m=512MiB, t=2, p=1** | **新增需求** | Spike 6 实测定参，保守路径 1.04s |
| 基本需求-3 | 拔卡自动卸载加密盘与隐藏盘 | **保留，并提升为安全需求** | Spike 6：模式 A 挂载期间驱动报告 `type=1(隐藏卷)` 且任意用户进程可查，卸载是否认性的组成部分 |
| —— | *新增*：建卷流程不可跳过的丢卡风险确认 | **新增需求** | Q-02 接受"卡丢=数据全没"的落地形式与举证依据 |
| —— | *新增*：建卷流程不可跳过的**口令遗忘**风险确认 | **新增需求** | Q-06 硬性需求 2，可与上一行合并为一屏两项 |
| —— | *新增*：口令强度校验与强度反馈 | **新增需求** | Q-06 硬性需求 3，弱口令直接抵消 Argon2id 防护 |
| —— | *新增*：Argon2id 派生，参数按目标机型压测（含入 P-002 的 5 秒预算） | **新增需求** | Q-06 硬性需求 1 |
| —— | ~~*新增*：模式 A / 模式 B 登录入口 UI 完全一致，不得暴露"另有口令"~~ | **已由上方"单一密码框"取代** | Q-07 的单密码框方案使该分叉在设计上不存在，无需再靠 UI 评审兜底 |
| —— | *新增*：`ICardStore` 按单 blob 抽象，多 PIN 降级为可选能力位 | **新增需求** | 见 N-02 第五节，避免硬件到货后返工 |
| —— | *新增*：两个容器**独立创建**，隐藏盘容器可后补（天然形成三档形态） | **新增需求🔴** | 见 8.3。若每个用户都有诱饵结构，否认性归零 |
| —— | *新增*：**档二与档三在磁盘、UI、本地状态三层不可区分**，含逐字节比对验收 | **新增需求🔴** | 见 8.4。本地状态层是最易疏漏的击穿点 |
| —— | *新增*：模式 C"更新诱饵"功能（自动派生密码、自动开保护、显示剩余可写空间） | **新增需求** | 见 8.5。VeraCrypt 原生仅一个复选框，个人用户无法驾驭 |
| —— | *新增*：建卷向导明示容器容量与隐藏卷尺寸**建成后不可调整** | **新增需求** | 见 N-05 技术附注第 4 点，VeraCrypt 无缩容、Expander 拒绝含隐藏卷的容器 |
| F-045 | 支持多个隐藏卷 | **删除或改为多容器实现** | 一个容器仅 2 个卷头槽位，原生不支持。闭环 v1 的 G-03 |
| —— | *新增*：**挂载顺序强制「先容器 B 后容器 A」**，B 失败即返回认证失败 | **新增需求🔴** | 见 N-06。容器 A 输任何密码都挂得上，先挂 A 会闪出盘符 |
| —— | *新增*：**档一下容器 A 的密码改为 `KDF(S, 用户输入)`**；升档时用 `ChangePwd()` 改写为 `KDF(S,"diskA")` | **新增需求🔴** | 见 N-06。否则默认档位下输任何密码均可登录 |
| Q-01-1 | 模式 B 后在隐藏卷写时间戳告警 | **实现路径作废，需求保留**：改为隐藏卷完整性校验 + 卡上单调计数器 | 见 N-07。模式 B 物理上不持有隐藏卷密码，写不进去 |
| —— | *新增*：USB 卡须支持**可写的持久计数器**（采购验收项） | **新增需求🔴** | 见 N-07。继"多 PIN 降级为可选"之后对卡的第二项要求 |
| —— | *新增*：**模式 C 需用户再输一次胁迫密码**（外层卷密码 = `KDF(S,P_duress)`） | **新增需求** | 见 8.2 补充。8.5 初稿仅写了隐藏卷密码自动派生 |
| —— | *新增*：明文导出的**风险确认屏 + 隐藏盘导出二次确认 + 导出动作零本地痕迹** | **新增需求🔴** | Q-04 可验收项 1-3。第 3 项属我方实现义务，纳入 8.4 逐字节比对范围 |
| R-02 | 全程无需管理员权限 | **适用范围收窄**：挂载/卸载/查询无需，**建卷与模式 C 需要** | 见 N-08。`FSCTL_GET_VOLUME_BITMAP` 要求提权 |
| —— | *新增*：**建卷流程无条件提权**（与档位无关） | **新增需求🔴** | 见 N-08。否则档二不提权、档三提权，UAC 行为本身泄露档位，违反 8.4 |
| —— | *新增*：**模式 C 亦需提权**，且**禁止用"空闲簇总数"替代尾部连续块扫描** | **新增需求🔴** | 见 N-08。实测朴素估算在碎片场景高估 **40.9%**，算多了即写坏隐藏卷 |

---

## 六、P0 实测数据（2026-08-15）

环境：Windows 11 Home 26200 / VeraCrypt 1.26.29 / 驱动版本 `0x126`
工具：`spike/vcspike.c`（clean-room 实现，不含任何 VeraCrypt 代码）

### Spike 1：IOCTL 挂载链路 ✅

| 项 | 结果 |
|---|---|
| IOCTL 通道连通 | ✅ |
| 挂载 / 卸载 / 状态查询 | ✅ 全部成功 |
| 盘符可用性 | ✅ 读写、子目录、嵌套文件正常 |
| 卸载后不可见 | ✅ 资源管理器与磁盘管理均无记录 |
| 数据持久性 | ✅ 重新挂载数据完好 |
| 错误密码拒绝 | ✅ `nReturnCode=3` |
| 挂载耗时 | **1.42~1.47 秒** |
| 卸载耗时 | **73~106 毫秒** |
| **全程未启动 VeraCrypt.exe** | ✅ |

**验证的《基本需求》条目**：
- 第 2 条（不加载时看不到盘符）→ ✅
- 第 4 条（扫描是随机数）→ ✅ 容器前 1KB 有 252/256 种不同字节值
- 第 6 条（文件操作与常规一致）→ ✅

### Spike 2：隐藏卷双模式 ✅

| 项 | 结果 |
|---|---|
| 隐藏卷密码挂载 | ✅ `type=1`，49.88 MB |
| 外层卷密码挂载 | ✅ `type=2`，199.75 MB |
| 同容器两套数据隔离 | ✅ |
| 隐藏卷文件不泄露 | ✅ 外层视角完全不可见 |
| 保护模式下写满外层 | ✅ 隐藏卷 6/6 文件校验和一致 |

`volumeType` 取值实测确认：**0=普通卷，1=隐藏卷，2=外层卷**

> **🔴 2026-08-17 Spike 6 订正**：上表"外层卷密码挂载 → `type=2`"**不准确**。
> `type=2` **仅在开启隐藏卷保护（`mount-outer`）时出现**；
> **模式 B 不开保护，驱动报告的是 `type=0`（普通卷）**。实测三种方式：
>
> | 挂载方式 | 模式 | type |
> |---|---|---|
> | 外层密码，不开保护 | **B 胁迫** | **`0` 普通卷** |
> | `mount-outer`，开保护 | C 维护 | `2` 外层卷 |
> | 隐藏卷密码 | A 正常 | `1` 隐藏卷 |
>
> **✅ 这对否认性是利好**：模式 B 挂出的卷在驱动属性中与普通卷完全一致，
> 无任何"外层卷"痕迹 —— 8.4 不可区分性又多一层免费保障。
>
> **⚠️ 但模式 A 有活体泄露**：驱动明确报告 `type=1`，
> 而 `GET_MOUNTED_VOLUMES` 是 `FILE_ANY_ACCESS`（R-02），**任何普通用户进程都能查到**。
> 即真实隐藏盘挂载期间，同机任意程序可看出"有隐藏卷正挂着"。
> 由此**"拔卡即卸载"（《基本需求》第 3 条）不只是便利功能，而是否认性的组成部分**。

### 尚未执行

| Spike | 内容 | 阻塞因素 |
|---|---|---|
| ~~3a~~ | ~~建卷工具裁剪可行性（源码分析）~~ | **✅ 2026-08-16 完成，见 N-01** |
| ~~3b~~ | ~~建卷工具**编译验证**~~ | **✅ 2026-08-19 完成**，见 `spike/SPIKE3B-建卷工具编译验证.md`。里程碑 1、2 均通过：改造版 FormatDLL 无界面建出隐藏卷容器，三种挂载方式 `type` 与容量全部符合预期（1/0/2）。改动仅 2 文件 30 行，未触碰 `Tcformat.c`。**N-01 的解决方案已由端到端实测证实**。附带发现 **N-08（建卷需管理员权限）** |
| 4 | **USB 卡多 PIN 能力** | 等硬件到货/供应商答复。**2026-08-17 起降级为非阻塞**：采用 N-02 第四节的派生方案后，其结论只影响是否追加纵深防御 |
| 5 | 分区形态预研（V-01） | v2.0 议题 |
| ~~6~~ | ~~双容器 + Argon2id 端到端耗时实测~~ | **✅ 2026-08-17 完成，见 `spike/SPIKE6-双容器挂载性能.md`**。P-002 成立（模式 A 3.63s），但须显式指定 PRF；并行挂载经实测无效 |

---

## 七、修订后的排期

| 阶段 | 内容 | 周期 |
|---|---|---|
| P0 | Spike 1、2、3a、3b、6 ✅ 已完成；Spike 4（非阻塞）、5（v2.0）待做 | **✅ 基本结束** |
| P1 | 卡通信封装、VolumeProvider + 容器实现、建卷工具、插拔监听、最小 UI | 4-5 周 |
| P2 | 注册/登录/忘记密码、三模式策略、建卷向导、备份、日志分离、UI 全量 | 5-6 周 |
| P3 | 异常路径加固、混淆反调试、EV 签名、安装包、多机与杀软测试 | 3-4 周 |

**合计约 3.5-4 个月。EV 证书采购需并行启动。**

排期相比 v1 的变化：
- **减少**：无需 WHQL（原估 4-12 周不确定性）、无需提权设计、C-007 可能可删
- **增加**：独立建卷工具、三模式否认性设计、日志分离

**2026-08-17 追加（Q-06 选 A 的排期影响）**：
- **减少（关键路径）**：USB 卡采购从"最长、风险最高的外部依赖"降为普通采购件，
  不再有"采购不到需推翻 Q-01"的返工风险；Spike 4 移出 P0 阻塞项
- **增加（P1）**：Argon2id 派生模块 + 参数压测，约 2-3 天（含并入 P-002 五秒预算的验证）
- **增加（P2）**：口令输入 UI、强度校验、口令遗忘风险确认屏，合计约 3-4 天
  （原列的"模式 A/B 入口一致性专项评审"已被 Q-07 单密码框方案消除，不再需要）
- **净影响**：总工期不变（仍 3.5-4 个月），但**最长的外部依赖被移除，排期确定性显著提高**

**2026-08-17 追加（N-05 双容器 + Q-07 手动输密码的排期影响）**：
- **增加（P1）**：`VolumeProvider` 需支持多容器编排，约 1-2 天
  （~~挂载并行化~~ 经 Spike 6 实测无效，已删除；
  **P-002 五秒预算的实测复核已于 2026-08-17 由 Spike 6 提前完成并通过**，
  该性能风险点解除）
- **增加（P2）**：建卷向导需支持分别创建加密盘容器与隐藏盘容器、设定两个密码、
  引导预置诱饵，约 **2-3 天**（原估 3-5 天，因取消"一次建两个容器"的耦合而下调）
- **减少（P2）**：取消"插卡自动登录"链路（含自动挂载、免密流程），约 1-2 天
- **净影响**：P1/P2 合计净增约 1 周，**仍在原 3.5-4 个月区间内**，但 P1 的性能复核
  若不达标可能引发方案调整，**建议 P1 优先做该项实测**

**2026-08-17 再追加（第八节的排期影响）**：
- **增加（P2）**：分档形态的建卷分支与各档一致的 UI，约 **2-3 天**
  （原估 3-4 天，因分档改由"容器独立创建"天然形成、无需额外档位机制而下调）
- **增加（P2）**：模式 C"更新诱饵"功能 —— 自动派生密码、自动开保护挂载、
  剩余可写空间计算与显示、触发保护后的告警，约 4-5 天
- **增加（P3）**：**档二／档三不可区分性验收** —— 分别建库后逐字节比对配置／注册表／日志／
  最近使用记录等全部本地产物，约 2-3 天。**此项不可省略，是否认性的最后一道关**
- **净影响**：P2/P3 合计再增约 **1-1.5 周**（经 2026-08-17 两项下调后）。
  总工期仍在 3.5-4 个月区间但已偏上限，若客户对工期敏感，
  可讨论将模式 C 降级为 v1.1（代价：诱饵无法更新，
  需在建卷时以"本身不需更新的材料"填充，见 8.5 末段）

**2026-08-19 追加（Q-04 结案 + N-06 / N-07 的排期影响）**：

| 项 | 阶段 | 估时 | 说明 |
|---|---|---|---|
| 挂载顺序强制串行 + 失败即中止 | P1 | **0** | 本就是 `VolumeProvider` 编排逻辑的一部分，是约束不是新工作量 |
| 档一容器 A 绑用户输入 | P1 | 0.5 天 | 派生分支，逻辑简单 |
| 升档改写 A 的卷密码 | P1 | **1 天** | ✅ **可行性已于 2026-08-19 实测确认**（零弹窗、约 2.8s）。原估 1-2 天，因不确定性消除而下调 |
| 隐藏卷完整性校验（N-07 机制 1） | P2 | **2-3 天** | 校验清单维护 + 登录时核对 + 告警 UI |
| 卡上单调计数器（N-07 机制 2） | P2 | 1-2 天 | 依赖卡的写能力，**须先确认采购件支持** |
| 模式 C 追加密码输入 | P2 | 0.5 天 | 并入模式 C 原有的 4-5 天 |
| 明文导出 + 两道确认 + 零本地痕迹 | P2 | **2-3 天** | Q-04。零痕迹一项须并入 P3 的逐字节比对验收 |

- **减少**：Q-04 只做明文导出、不做加密备份选项，相比"两个选项"方案省约 2-3 天
- **净影响**：**合计净增约 1 周，总工期已达 4 个月上限。**
  🔴 **若客户对工期敏感，此时应正式提出将模式 C 降级为 v1.1**（可省 4-5 天），
  这是当前最大且风险最低的一块可裁剪项
- ⚠️ **N-07 的卡上计数器依赖硬件能力**，采购确认前不要排进关键路径；
  若卡不支持则直接删除该项（完整性校验可独立成立），减 1-2 天

**2026-08-19 再追加（Spike 3b 完成 + N-08 的排期影响）**：

- **减少（P0）**：Spike 3b 结案，**P0 阶段基本结束**。建卷工具的技术不确定性解除，
  P1 该项可按确定量排期，不再需要保留风险缓冲
- **减少（P1）**：建卷工具的实现量低于原估 —— 改造仅 2 文件 30 行且已验证，
  P1 的主要工作变成"包装成独立 exe + 进程间协议 + 错误码映射"，约省 **2-3 天**
- **增加（P1）**：建卷流程的 UAC 提权设计与实现，约 **1 天**
  （R-02 曾取消该项，现按 N-08 部分恢复，但仅限建卷路径，比原设计简单得多）
- **增加（P2）**：模式 C 的提权处理，约 0.5 天，并入模式 C 原有工时
- **净影响**：**约省 1-1.5 天**，且 P0 提前结束。
  总工期仍在 4 个月上限，但**最大的技术风险项（建卷）已从"未验证"变为"已验证"**，
  排期确定性显著提高

---

## 八、模式与形态的完整设计（2026-08-17 新增）

> 本节整合 R-01（三模式）、N-05（双容器）、Q-06/Q-07（密码派生与手动输入）的最终形态，
> 并新增分档设计与不可区分性要求。**实现时以本节为准。**

### 8.1 先厘清：什么是"隐藏卷保护"

这是 VeraCrypt 挂载时的一个开关（`MOUNT_STRUCT.bProtectHiddenVolume`）。理解它的前提是：

**驱动默认不知道隐藏卷在哪。** 挂载外层卷时它看到的就是一个普通卷，尾部那片区域在它眼里是空闲空间。
**除非你把隐藏卷密码也一并提供** —— 驱动据此算出隐藏卷位置，并在 I/O 路径上拦截写入。

驱动实现（`Driver/EncryptedIoQueue.c:1005-1014`）：写请求只要与
`[hiddenVolumeOffset, +hiddenVolumeProtectedSize)` 有重叠，直接
`CompleteOriginalIrp(item, STATUS_INVALID_PARAMETER, 0)` 拒绝。

**副作用（必须知道）**：保护一旦被触发，**整卷立即变为不可写**。
`EncryptedIoQueue.c:996-998` 原注释：

> `// Do not allow writing to this volume anymore. This is to fake a complete volume`
> `// or system failure (otherwise certain kinds of inconsistency within the file`
> `// system could indicate that this volume has used hidden volume protection).`

即官方刻意伪装成"整机故障"。但该伪装并不彻底 —— Spike 2 实测到的
"写入全部成功、卸载后文件全消失、空间却被占用"正是 R-01 判定其会暴露隐藏卷的依据。

**代价**：开启保护必须提供隐藏卷密码。

### 8.2 三个使用场景

| | 谁在操作 | 用户输入 | 挂出什么 | 保护 | 写入安全 |
|---|---|---|---|---|---|
| **A 正常使用** | 用户 | 真实密码 | 加密盘 + **真**隐藏盘 | 不涉及 | ✅ |
| **B 胁迫交出** | 对手在场 | 胁迫密码 | 加密盘 + **诱饵**隐藏盘（容器 B 外层卷） | **关** | ❌ 对手写入会覆盖真隐藏盘 |
| **C 维护诱饵** | 用户，独处 | 真实密码 + 主动触发 | 容器 B 的**外层卷** | **开** | ✅ 驱动硬拦 |

**关键设计性质**：模式 B 下软件**根本算不出隐藏卷密码**
（`KDF(S, P_duress)` 只能得到外层卷密码），因此**即便对手强迫软件"开启保护"，它也做不到**
—— 不是拒绝执行，是物理上不持有该数据。这是比"程序逻辑上不开"强得多的保证。

**模式 B 绝不开保护的第二个理由**：保护被触发后整卷转为不可写，对手会看到
"盘还显示有剩余空间却写不进去"的反常，等于暴露。

**模式 C 的流程约束**：同一容器不能挂载两次（见 N-05），
故模式 C 需先卸载容器 B 的隐藏卷，再以保护模式挂载其外层卷。

> **🔴 2026-08-19 补充：模式 C 需要两个密码，8.5 初稿只写了一个。**
> 以保护模式挂载外层卷，需同时提供：
> - **外层卷密码** = `KDF(S, P_duress)` —— 用户已用 `P_real` 登录，**必须再输一次胁迫密码**
> - **隐藏卷密码** = `KDF(S, P_real)` —— 软件自动派生，用户无感（8.5 已述）
>
> 即模式 C 的流程中必然多出一个密码输入框。
> **不影响"用户全程不接触保护模式概念"这一目标**（对用户呈现为"请再次验证身份"即可），
> 但第七节中模式 C 的 4-5 天工作量须把它算进去。

### 8.3 🔴 分档设计：否认性的真正落点

**问题**：若产品对每个用户都创建"诱饵 + 真隐藏盘"的结构，
则对手确知**必然存在隐藏层**，会一直逼问 —— **否认性归零**。

**原理澄清**：否认性要藏的**不是"该功能存在"，而是"这个用户启用了它"**。
VeraCrypt 的隐藏卷功能完全公开，其否认性依然成立，
正是因为无人能证明某个容器**有**隐藏卷。

**因此产品必须真的支持"不启用"，且为默认状态。**
由于两个容器独立创建（见 N-05 订正），分档天然形成三级，无需额外的档位开关：

| 档位 | 用户建了什么 | 胁迫时展示 | 用户可声称 |
|---|---|---|---|
| **一（默认）** | 只建加密盘容器 | 加密盘 | "我没建过隐藏盘" |
| **二** | 加密盘 + 隐藏盘容器（**普通卷**） | 加密盘 + 隐藏盘（**就是真的**） | "隐藏盘就是你看到的这个" |
| **三（完整）** | 加密盘 + 隐藏盘容器（外层 + 隐藏） | 加密盘 + **诱饵**隐藏盘 | 同上 |

**关键**：档二与档三**在磁盘上完全不可区分** ——
普通卷与外层卷结构相同，隐藏卷头槽位里是有效头还是随机数，
不知道密码无法分辨（依据见 N-05 技术附注第 1 点）。
用户处于档三时可主张自己是档二，**对手无法证伪**。

档一则是另一种防线：隐藏盘容器文件根本不存在，无从追问。

> **🔴 2026-08-19：档一存在一个已识别的实现缺口 —— 该档没有认证锚点。**
> 容器 A 的密码不依赖用户输入，而档一又没有容器 B 可供校验，
> 导致**输任何密码均可登录**。**档一是默认档位，影响面最大。**
> 解法（档一下 A 改绑用户输入，升档时改写卷密码）见 **N-06**。

> **档位越多，对手越难断定用户处在哪一档。**
> 且这三档不是我们额外设计出来的形态，而是"两个容器独立创建"的自然结果 —— 零额外机制。

### 8.4 🔴 不可区分性要求：三个层面缺一不可

> 下述要求针对**档二与档三**（档一无隐藏盘容器，不涉及）。

| 层面 | 保障方 | 要求 |
|---|---|---|
| **磁盘** | ✅ VeraCrypt 格式已保证 | 无需额外工作 |
| **挂载属性** | ✅ VeraCrypt 驱动已保证（Spike 6 实测） | 模式 B 报告 `type=0`，与普通卷不可区分 |
| **UI** | ⚠️ 我方实现 | 见下 |
| **本地状态** | 🔴 **我方实现，最易疏漏** | 见下 |
| **运行期** | ⚠️ 我方实现 | 模式 A 挂载期间驱动报告 `type=1`，任意用户进程可查（见第六节）→ **必须靠"拔卡即卸载"压缩暴露窗口** |

**UI 层要求**：档三（完整）下的"胁迫模式界面"必须是**档二界面的完美复制**。

- 不得出现灰掉的按钮、"高级功能"占位、升级提示
- 设置页不得出现处于关闭态的相关开关项
- 不得有"当前处于 XX 模式"之类的任何指示
- **要么整个功能不存在，要么就是完全一致的两套界面**

**本地状态层要求（新增，此前所有版本均未识别）**：
配置文件、注册表、日志、最近使用记录、盘符历史等本地痕迹，
**在档二与档三下必须不可区分**。

- ❌ 禁止写入 `mode=full` 一类的档位标记
- ❌ 禁止记录隐藏盘容器的隐藏卷尺寸、真实可用容量等可推断出隐藏层存在的数据
- ❌ 日志禁止出现"挂载隐藏卷""启用保护"等事件（与排期中"日志分离"一项合并考虑）
- ❌ **（2026-08-19 新增）明文导出动作不得留任何痕迹** —— 日志、最近使用记录、
  上次导出路径一律不写。见 **Q-04 可验收项 3**。
  ⚠️ 这是本层**最容易被当成普通功能而漏掉**的一条：备份是常规功能，
  几乎所有软件都会记住上次导出目录，一记就击穿
- ✅ 验收方式：分别按档二、档三建库后**逐字节比对**所有本地产物，差异必须为零或与档位无关

> **⚠️ 这一层比 UI 更危险**：按钮藏没藏好可以评审出来，
> 配置里多写一个字段则极易漏过，且一旦泄露即彻底击穿否认性。

**与 Q-01 硬性需求 3 的张力及其处理**：Q-01 要求"产品文档与首次使用引导中明示胁迫模式风险"。
按 8.3 的原理，**公开机制本身不伤否认性**，故该要求可保留；
但这些说明**只能出现在"用户正在创建含隐藏卷的隐藏盘容器"（即选择档三）的流程内**，
**档一、档二的任何界面与文档路径中都不得出现**，否则等于宣告"本产品每个用户都可能有隐藏层"。

### 8.5 模式 C 的产品化（VeraCrypt 原生做法对比）

**VeraCrypt 的做法：基本不管，全甩给用户。**

- **建卷时**：向导有专门一步 `Outer Volume Contents`，建完外层卷后自动挂载并停下等用户拷贝。
  官方对诱饵的定义（`Common/Language.xml:529`）：
  > *copy some **sensitive-looking files that you actually do NOT want to hide***
- **建卷后**：**只有挂载对话框里的一个复选框**（`Language.xml:329`）：
  > *Protect hidden volume against damage caused by writing to outer volume*

  勾选后要求再输一遍隐藏卷密码。**没有向导、没有维护入口、没有任何引导。**
- **全部反馈**：保护生效时显示 `Hidden Volume Protected`；
  保护被触发后卷列表类型栏变为 **`Outer(!)`** —— 一个感叹号就是全部警告。
- **唯一前瞻提醒**（`Language.xml:1006`）：选隐藏卷尺寸时提示
  "以后还想往外层卷加数据的话，隐藏卷应选小一点"。

**其隐含假设是"用户是懂行的人"。Q-03 已确认本产品面向个人用户，该假设不成立。**

**我方设计**：

| | VeraCrypt | 本产品 |
|---|---|---|
| 入口 | 挂载选项里的复选框 | **仅档三**、且真实密码登录后才渲染的"更新诱饵"功能 |
| 隐藏卷密码 | 用户手输 | **软件用真实密码自动派生**，用户全程不接触"保护模式"概念 |
| 外层卷密码 | 用户手输 | **须用户再输一次胁迫密码**，见下方 2026-08-19 补充 |
| 剩余可写空间 | 不显示 | 🆕 **明确显示"还可添加 X MB"**（算法同 Q-01 硬性需求 2） |
| 触发保护后 | 类型栏变 `Outer(!)` | 明确告警 + 说明后果 + 指引 |
| 何时该维护 | 无提示 | 可按诱饵文件时间戳老化程度提醒 |

> **"剩余可写空间"是本产品明显优于 VeraCrypt 之处**，且一套算法服务两个场景：
> 建卷时校验诱饵是否填够，模式 C 时告知还能加多少。

**诱饵内容的设计建议**：优先选择**本身就不需要更新**的材料（旧照片、扫描件、往年单据），
从源头降低模式 C 的使用频率 —— 每少用一次，就少一次被观察到的机会。

---

## 附：本文档结论的依据来源

- VeraCrypt 官方仓库源码（`Apidrvr.h`、`Password.h`、`Tcdefs.h`、`Volumes.h`、`Dlgcode.c`）
- **2026-08-17 新增引用**（均位于 `spike/_vcref/src/`）：
  - `Common/Volumes.h:51-77` —— 卷头槽位布局，证明一个容器最多 2 个卷
  - `Common/Format.c:125-131` —— 隐藏卷数据区偏移计算，证明其位于宿主尾部
  - `Format/Tcformat.c:9758-9763` —— `DetermineMaxHiddenVolSize()` 的 0.5%／10MB 自留余量及其注释
  - `Format/Tcformat.c:9884, 9922` —— 簇位图扫描求"尾部连续空闲块"的注释
  - `Driver/EncryptedIoQueue.c:990-1014` —— 隐藏卷保护的写入拦截与"伪装整机故障"注释
  - `ExpandVolume/WinMain.cpp:62-63`、`DlgExpandVolume.cpp:572-577` —— Expander 拒绝含隐藏卷的容器
  - `Common/Language.xml:329, 529, 1006` —— 官方对诱饵的定义、保护复选框、容量前瞻提醒
- **2026-08-19 新增引用**：
  - `Common/Password.c:190` —— `ChangePwd()` 函数签名，证明改写卷密码的能力现成且非 `static`（N-06）
  - `FormatDLL/x64/Release/Password.obj` —— 证明 `Password.c` 已被 FormatDLL 工程编译，可直接调用（N-06）
  - `Common/Dlgcode.c:7421` —— `UserEnrichRandomPool()` 用 `DialogBoxParamW` 弹熵采集框，只看 `IsRandomPoolEnrichedByUser()` 而不看 `Silent`（N-06 的第二项抑制措施）
  - `Format/Tcformat.c:9997` —— `ScanVolClusterBitmap()` 以 `GENERIC_READ` 打开卷设备（N-08）
  - `Format/Tcformat.c:9870-9930` —— `DetermineMaxHiddenVolSize()` 的 FAT / NTFS 分支，两者均调用 `ScanVolClusterBitmap()`（N-08）
  - 本地实测：`spike/spike3b_hidden.cpp`（两阶段建卷编排）、`spike/probe_bitmap.c`（FSCTL 访问掩码探针，四组对照）、`spike/verify3b.bat`（三种挂载方式验收）、`spike/spike3b_chpwd.cpp`（`ChangePwd` 无界面验证）、`spike/frag_test.cpp`（碎片化测试，量化朴素估算的高估幅度）
- VeraCrypt-SDK 官方仓库（`VeraCryptFormatSDK.h`、README、LICENSE）
- VeraCrypt 官方文档：License、Hidden Volume、Protection of Hidden Volumes、Command Line Usage
- GitHub Issue #1305（命令行无法创建隐藏卷）
- 本地实测：`spike/vcspike.c` + `spike/README.md`
